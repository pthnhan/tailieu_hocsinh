\section{SỰ ĐỒNG BIẾN, NGHỊCH BIẾN CỦA HÀM SỐ}
	\subsection{Tóm tắt lý thuyết}
	\subsubsection{Tính đơn điệu của hàm số}
	\begin{dn}
		Cho hàm số $y = f(x)$ xác định trên $\mathscr{K}$ ($\mathscr{K}\subset\mathbb{R}$ là một khoảng). Ta nói \\
		$\bullet$ Hàm số $y = f(x)$ \textbf{đồng biến} (tăng) trên $\mathscr{K}$ nếu với mọi cặp $x_1$, $x_2$ thuộc $\mathscr{K}$ mà $x_1$ nhỏ hơn $x_2$ thì $f\left(x_1 \right)$ nhỏ hơn $f\left(x_x \right)$, tức là $x_1 < x_2 \Rightarrow f\left(x_1 \right) < f\left(x_2 \right)$. \\
		$\bullet$ Hàm số $y = f(x)$ \textbf{nghịch biến} (giảm) trên $\mathscr{K}$ nếu với mọi cặp $x_1$, $x_2$ thuộc $\mathscr{K}$ mà $x_1$ nhỏ hơn $x_2$ thì $f\left(x_1 \right)$ lớn hơn $f\left(x_x \right)$, tức là $x_1 < x_2 \Rightarrow f\left(x_1 \right) > f\left(x_2 \right)$.
	\end{dn}
	\begin{dl}
		Cho hàm số $y = f(x)$ có đạo hàm trên $\mathscr{K}$. 
\begin{itemize}
	 \item Nếu $f'(x) > 0$ với mọi $x$ thuộc $\mathscr{K}$ thì hàm số $f(x)$ đồng biến trên $\mathscr{K}$. 
	\item Nếu $f'(x) < 0$ với mọi $x$ thuộc $\mathscr{K}$ thì hàm số $f(x)$ nghịch biến trên $\mathscr{K}$.
\end{itemize}
	\end{dl}
	\begin{dl}
		Cho hàm số $y = f(x)$ có đạo hàm trên $\mathscr{K}$.  
 Nếu $f'(x) \geq 0$ $\left(f'(x) \leq 0 \right)$ với mọi $x$ thuộc $\mathscr{K}$ và $f'(x) = 0$ chỉ tại một số hữu hạn điểm thì hàm số $f(x)$ đồng biến (nghịch biến) trên $\mathscr{K}$.\end{dl}
	\subsubsection{Quy tắc xét tính đơn điệu của hàm số}
	\begin{itemize}
		\item	Tìm tập xác định.
		\item	Tính đạo hàm $f'(x)$. Tìm các điểm $x_i \,(i = 1, 2, ..., n)$ mà tại đó đạo hàm bằng $0$ hoặc không xác định.
		\item	Sắp xếp các điểm $x_i$ theo thứ tự tăng dần và lập bảng biến thiên.
		\item	Nêu kết luận về các khoảng đồng biến, nghịch biến của hàm số.
	\end{itemize}
	
	\subsection{Các dạng toán}
	
	\begin{dang}{Xét sự đồng biến - nghịch biến của hàm số}
		Để xét sự đồng biến, nghịch biến của hàm số $y=f(x)$ ta thực hiện các bước giải như sau:
		\begin{itemize}
			\item \textbf{\underline{Bước 1}:} Tìm tập xác định $\mathscr{D}$ của hàm số.
			\item \textbf{\underline{Bước 2}:} Tính $y'$. Tìm các điểm thuộc $\mathscr{D}$ mà tại đó $y'=0$ hoặc $y'$ không xác định.
			\item \textbf{\underline{Bước 3}:} Lập bảng biến thiên của hàm số.
			\item \textbf{\underline{Bước 4}:} Nêu kết luận về các khoảng đồng biến và nghịch biến của hàm số.
		\end{itemize}
	\end{dang}
	
	\begin{vd}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=-x^3+6x^2-9x+4$.
		\loigiai{
			Hàm số $y=-x^3+6x^2-9x+4$ có tập xác định $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=-3x^2+12x-9$. Cho $y'=0 \Leftrightarrow -3x^2+12x-9=0 \Leftrightarrow \left[\begin{aligned}
			&x=1 \\
			&x=3.
			\end{aligned}\right. $\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$1$,$3$,$+\infty$}
				\tkzTabLine{,-,0,+,0,-,}
				\tkzTabVar{+/$+\infty$,-/ $0$/,+/$4$,-/$-\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số nghịch biến trên mỗi khoảng $(-\infty;1)$, $(3;+\infty)$ và đồng biến trên khoảng $(1;3)$.
		}
	\end{vd}
	\begin{vd}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=-x^4+4x^2-3$.
		\loigiai{
			Tập xác định của hàm số $y=-x^4+4x^2-3$ là $ \mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=-4x^3+8x$.
			Cho $y'=0 \Leftrightarrow -4x^3+8x=0 \Leftrightarrow 4x(-x^2+2)=0$\\
			\centerline{$ \Leftrightarrow \left[\begin{aligned}
				&4x=0 \\
				&-x^2+2=0
				\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
				&x=0 \\
				&x^2=2
				\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
				&x=0 \\
				&x=\pm \sqrt{2}.
				\end{aligned}\right. $}
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-\sqrt{2}$,$0$,$\sqrt{2}$,$+\infty$}
				\tkzTabLine{,+,0,-,0,+,0,-,}
				\tkzTabVar{-/$-\infty$,+/ $1$/,-/$-3$,+/$1$,-/$-\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số đồng biến trên mỗi khoảng $(-\infty;-\sqrt{2})$ và $(0;\sqrt{2})$,\\
			\indent{ } hàm số nghịch biến trên mỗi khoảng $(-\sqrt{2};0)$ và $(\sqrt{2};+\infty)$.
		}
	\end{vd}
	\begin{vd}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=x^4-6x^2+8x+1$.
		\loigiai{
			Hàm số $y=x^4-6x^2+8x+1$ có tập xác định $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=4x^3-12x+8=0=4(x-1)^2(x+2)$.\\
			Cho $y'=0 \Leftrightarrow 4(x-1)^2(x+2)=0 \Leftrightarrow \left[\begin{aligned}
			&x=-2 \\
			&x=1.
			\end{aligned}\right.$\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-2$,$1$,$+\infty$}
				\tkzTabLine{,-,0,+,0,+,}
				\tkzTabVar{+/$+\infty$,-/ $-23$/,R,+/$+\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số nghịch biến trên khoảng $(-\infty;-2)$ và đồng biến trên khoảng $(-2;+\infty)$.
		}
	\end{vd}
	\begin{vd}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=\dfrac{3-2x}{x+7}$.
		\loigiai{
			Hàm số $y=\dfrac{3-2x}{x+7}=\dfrac{-2x+3}{x+7}$ có tập xác định $ \mathscr{D}=\mathbb{R}\setminus \{-7\}$.\\
			Ta có $y'=\dfrac{-17}{(x+7)^2}<0,\forall x \neq -7$.\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=4]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-7$,$+\infty$}
				\tkzTabLine{,-,d,-,}
				\tkzTabVar{+/$-2$,-D+/$-\infty$/$+\infty$,-/$-2$}
				\end{tikzpicture}
			\end{center}
			Hàm số đã cho nghịch biến trên mỗi khoảng $(-\infty;-7)$ và $(-7;+\infty)$.
		}
	\end{vd}
	\begin{vd}%[Dương Phước Sang]%[2D1K1]
		Tìm các khoảng đơn điệu của hàm số $y=\dfrac{x^2-x+1}{x-1}$.
		\loigiai{
			Hàm số $y=\dfrac{x^2-x+1}{x-1}$ có tập xác định $\mathscr{D}=\mathbb{R}\setminus \{1\}$.\\
			Ta có $y'=\dfrac{x^2-2x}{(x-1)^2},\forall x\in \mathscr{D}$.\\
			Cho $y'=0 \Leftrightarrow \dfrac{x^2-2x}{(x-1)^2}=0 \Leftrightarrow x^2-2x=0 \Leftrightarrow \left[\begin{aligned}
			&x=0 \\
			&x=2.
			\end{aligned}\right. $\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$0$,$1$,$2$,$+\infty$}
				\tkzTabLine{,+,$0$,-,d,-,$0$,+,}
				\tkzTabVar{-/$-\infty$,+/$-1$,-D+/$-\infty$/$+\infty$,-/$3$,+/$+\infty$}
				\end{tikzpicture}
			\end{center}
			Hàm số đồng biến biến trên mỗi khoảng $(-\infty;0)$ và $(2;+\infty)$\\
			Hàm số nghịch biến trên mỗi khoảng $(0;1)$ và $(1;2)$.
		}
	\end{vd}
	\begin{vd}%[Dương Phước Sang]%[2D1K1]
		Tìm các khoảng đồng biến, nghịch biến của hàm số $y=x+\sqrt{16-x^2}$.
		\loigiai{
			Tập xác định: $\mathscr{D}=[-4;4]$.\\
			Đạo hàm: $y'=1-\dfrac{x}{\sqrt{16-x^2}}=\dfrac{\sqrt{16-x^2}-x}{\sqrt{16-x^2}}$.\\
			Cho $y'=0 \Leftrightarrow \left\{\begin{aligned}
			&\sqrt{16-x^2}=x \\
			&16-x^2>0
			\end{aligned}\right. \Leftrightarrow \left\{\begin{aligned}
			&x\geqslant 0 \\
			&0<16-x^2=x^2
			\end{aligned}\right. $
			$ \Leftrightarrow \left\{\begin{aligned}
			&x>0 \\
			&x^2=8
			\end{aligned}\right. \Leftrightarrow  x=2\sqrt{2}$.\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-4$,$2\sqrt{2}$,$4$,$+\infty$}
				\tkzTabLine{,h,d,+,$0$,-,d,h,}
				\tkzTabVar{+H/,-/$-4$,+/$4\sqrt{2}$,-H/$4$,}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số đồng biến trên khoảng $(-4;2\sqrt{2})$ và nghịch biến trên khoảng $(2\sqrt{2};4)$.
		}
	\end{vd}
	\begin{center}
		\textbf{BÀI TẬP TỰ LUYỆN}
	\end{center}
	\begin{bt}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=-2x^4+4x^2$.
		\loigiai{
			Tập xác định của hàm số là $ \mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=-8x^3+8x$.
			Cho $y'=0 \Leftrightarrow -8x^3+8x=0 \Leftrightarrow 8x(-x^2+1)=0$\\
			\centerline{$ \Leftrightarrow \left[\begin{aligned}
				&8x=0 \\
				&-x^2+1=0
				\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
				&x=0 \\
				&x^2=1
				\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
				&x=0 \\
				&x=\pm 1.
				\end{aligned}\right. $}
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-1$,$0$,$1$,$+\infty$}
				\tkzTabLine{,+,0,-,0,+,0,-,}
				\tkzTabVar{-/$-\infty$,+/ $2$/,-/$0$,+/$2$,-/$-\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số đồng biến trên mỗi khoảng $(-\infty;-1)$ và $(0;1)$,\\
			\indent{ } hàm số nghịch biến trên mỗi khoảng $(-1;0)$ và $(1;+\infty)$.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=x^4-2x^2-3$.
		\loigiai{
			Tập xác định của hàm số là $ \mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=4x^3-4x$.
			Cho $y'=0 \Leftrightarrow 4x^3-4x=0 \Leftrightarrow 4x(x^2-1)=0$\\
			\centerline{$ \Leftrightarrow \left[\begin{aligned}
				&4x=0 \\
				&x^2-1=0
				\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
				&x=0 \\
				&x^2=1
				\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
				&x=0 \\
				&x=\pm 1.
				\end{aligned}\right. $}
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-1$,$0$,$1$,$+\infty$}
				\tkzTabLine{,-,0,+,0,-,0,+,}
				\tkzTabVar{+/$+\infty$,-/$-4$ /,+/$-3$,-/$-4$,+/$+\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số đồng biến trên mỗi khoảng $(-1;0)$ và $(1;+\infty)$ ,\\
			\indent{ } hàm số nghịch biến trên mỗi khoảng $(-\infty;-1)$ và $(0;1)$.
		}
	\end{bt}
	
	\begin{bt}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=x^4+4x^3-1$.
		\loigiai{
			Hàm số đã cho xác định trên $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=4x^3+12x^2=0=4x^2(x+3)$.\\
			Cho $y'=0 \Leftrightarrow 4x^2(x+3)=0 \Leftrightarrow \left[\begin{aligned}
			&x=0 \\
			&x=-3.
			\end{aligned}\right.$\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-3$,$0$,$+\infty$}
				\tkzTabLine{,-,0,+,0,+,}
				\tkzTabVar{+/$+\infty$,-/$-28$ /,R,+/$+\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số nghịch biến trên khoảng $(-\infty;-3)$ và đồng biến trên khoảng $(-3;+\infty)$.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=x^4+4x+6$.
		\loigiai{
			Tập xác định: $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=4x^3+4$. Cho $y'=0 \Leftrightarrow 4x^3+4=0 \Leftrightarrow x=-1$.\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-1$,$+\infty$}
				\tkzTabLine{,-,0,+,}
				\tkzTabVar{+/$+\infty$,-/ $3$/,+/$+\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số nghịch biến trên khoảng $(-\infty;-1)$ và đồng biến trên khoảng $(-1;+\infty)$.
		}
	\end{bt}
	
	\begin{bt}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=x^3-x^2-x+1$.
		\loigiai{
			Hàm số đã cho xác định trên $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=3x^2-2x-1$. Cho $y'=0 \Leftrightarrow 3x^2-2x-1=0 \Leftrightarrow \left[\begin{aligned}
			&x=1 \\
			&x=-\dfrac{1}{3}.
			\end{aligned}\right. $\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/1,$y'$/0.7,$y$/2}
				{$-\infty$,$-\dfrac{1}{3}$,$1$,$+\infty$}
				\tkzTabLine{,+,0,-,0,+,}
				\tkzTabVar{-/$-\infty$,+/$\dfrac{32}{27}$,-/$0$,+/$+\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số đồng biến trên mỗi khoảng $\left(-\infty;-\dfrac{1}{3}\right)$, $(1;+\infty)$ và nghịch biến trên khoảng $\left(-\dfrac{1}{3};1\right)$.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1B1]
		Xét sự đồng biến, nghịch biến của hàm số $y=x^3+3x^2+3x+2$.
		\loigiai{
			Hàm số đã cho xác định trên $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=3x^2+6x+3$. Cho $y'=0 \Leftrightarrow 3x^2+6x+3=0 \Leftrightarrow x=-1$.\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-1$,$+\infty$}
				\tkzTabLine{,+,0,+,}
				\tkzTabVar{-/$-\infty$,R/,+/$+\infty$}
				
				\end{tikzpicture}
			\end{center}
			Vậy hàm số đồng biến trên $\mathbb{R}$.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của  hàm số $y=\sqrt{x^2-2x}$.
		\loigiai{
			Hàm số đã cho xác định khi và chỉ khi $x^2-2x\geqslant 0 \Leftrightarrow \left[\begin{aligned}
			&x\leq  0 \\
			&x\geqslant 2.
			\end{aligned}\right.$\\
			Tập xác định: $\mathscr{D}=(-\infty;0]\cup [2;+\infty)$.\\
			Ta có $y'=\dfrac{x-1}{\sqrt{x^2-2x}},\forall x\in (-\infty;0)\cup (2;+\infty)$.\\
			Hàm số không có đạo hàm tại $x=0$ và $x=2$.\\
			Cho $y'=0 \Leftrightarrow \dfrac{x-1}{\sqrt{x^2-2x}}=0 \Rightarrow x-1=0 \Leftrightarrow x=1 \notin \mathscr{D}$.\\
			Bảng biến thiên:
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$0$,$2$,$+\infty$}
				\tkzTabLine{,-,d,h,d,+,}
				\tkzTabVar{+/$+\infty$,-H/$0$/,-/$0$,+/$+\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số nghịch biến trên khoảng $(-\infty;0)$ và đồng biến trên khoảng $(2;+\infty)$.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1B1]
		Tìm các khoảng đơn điệu của hàm số $y=\dfrac{3x+1}{1-x}$.
		\loigiai{
			Hàm số xác định và liên tục trên $\mathscr{D}=\mathbb{R}\setminus \{1\}$.\\
			Ta có $y'=\dfrac{4}{(1-x)^2}>0,\forall x\ne 1$.\\
			Bảng biến thiên:
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=4]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$1$,$+\infty$}
				\tkzTabLine{,+,d,+,}
				\tkzTabVar{-/$-3$,+D-/$+\infty$/$-\infty$,+/$-3$}
				\end{tikzpicture}
			\end{center}
			Hàm số đã cho đồng biến trên các khoảng $(-\infty;1)$ và $(1;+\infty)$.
		}
	\end{bt}
	
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các khoảng đơn điệu của hàm số $y=\dfrac{-x^2+2x-1}{x+2}$.
		\loigiai{
			Hàm số đã cho xác định trên $\mathscr{D}=\mathbb{R}\setminus \{-2\}$.\\
			Ta có $y'=\dfrac{-x^2-4x+5}{(x+2)^2},\forall x\in \mathscr{D}$.\\
			Cho $y'=0 \Leftrightarrow \dfrac{-x^2-4x+5}{(x+2)^2}=0 \Leftrightarrow -x^2-4x+5=0 \Leftrightarrow \left[\begin{aligned}
			&x=-5 \\
			&x=1.
			\end{aligned}\right. $\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-5$,$-2$,$1$,$+\infty$}
				\tkzTabLine{,-,$0$,+,d,+,$0$,-,}
				\tkzTabVar{+/$+\infty$,-/$12$,+D-/$+\infty$/$-\infty$,+/$0$,-/$-\infty$}
				\end{tikzpicture}
			\end{center}
			Hàm số nghịch biến trên mỗi khoảng $(-\infty;-5)$ và $(1;+\infty)$\\
			Hàm số đồng biến trên mỗi khoảng $(-5;-2)$ và $(-2;1)$.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các khoảng đơn điệu của hàm số $y=\dfrac{x+2}{\sqrt{x^2-x+3}}$.
		\loigiai{
			Hàm số đã cho xác định khi $x^2-x+3>0$ (đúng với mọi $x\in \mathbb{R}$).\\
			Hàm số đã cho xác định trên $ \mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=\dfrac{\sqrt{x^2-x+3}-\dfrac{(x+2)(2x-1)}{2\sqrt{x^2-x+3}}}{x^2-x+3}=\dfrac{-5x+8}{2(x^2-x+2)\sqrt{x^2-x+2}}$.\\
			Cho $y'=0 \Leftrightarrow \dfrac{-5x+8}{2(x^2-x+2)\sqrt{x^2-x+2}}=0 \Leftrightarrow -5x+8=0 \Leftrightarrow x=\dfrac{8}{5}$.\\
			Bảng biến thiên:
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.9,$y'$/0.7,$y$/2}
				{$-\infty$,$\dfrac{8}{5}$,$+\infty$}
				\tkzTabLine{,-,$0$,+,}
				\tkzTabVar{-/$-1$,+/$\dfrac{6}{\sqrt{11}}$,-/$1$}
				\end{tikzpicture}
			\end{center}
			Hàm số đã cho đồng biến trên khoảng $\left(-\infty;\dfrac{8}{5}\right)$ và nghịch biến trên khoảng $\left(\dfrac{8}{5};+\infty \right)$.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các khoảng đơn điệu của hàm số $y=(4-3x)\sqrt{6x^2+1}$.
		\loigiai{
			Hàm số đã cho xác định trên $ \mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=-3\sqrt{6x^2+1}+\dfrac{6x(4-3x)}{\sqrt{6x^2+1}}=\dfrac{-36x^2+24x-3}{\sqrt{6x^2+1}}$.\\
			Cho $y'=0 \Leftrightarrow \dfrac{-36x^2+24x-3}{\sqrt{6x^2+1}}=0 \Leftrightarrow -36x^2+24x-3=0 \Leftrightarrow \left[\begin{aligned}
			&x=\dfrac{1}{2} \\
			&x=\dfrac{1}{6}.
			\end{aligned}\right. $\\
			Bảng biến thiên
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/1,$y'$/0.7,$y$/3}
				{$-\infty$,$\dfrac{1}{6}$,$\dfrac{1}{2}$,$+\infty$}
				\tkzTabLine{,+,$0$,-,$0$,+,}
				\tkzTabVar{-/$-\infty$,+/$\dfrac{7\sqrt{42}}{12}$,-/$\dfrac{5\sqrt{10}}{4}$,+/$+\infty$}
				\end{tikzpicture}
			\end{center}
			Hàm số đã cho đồng biến trên các khoảng $\left(-\infty;\dfrac{1}{6}\right)$ và $\left(\dfrac{1}{2};+\infty \right)$.\\
			Hàm số nghịch biến trên khoảng $\left(\dfrac{1}{6};\dfrac{1}{2}\right)$.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các khoảng đồng biến và nghịch biến của hàm số $y=|x^2-2x-3|$.
		\loigiai{
			Tập xác định của hàm số là $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y=|x^2-2x-3|=\sqrt{\left(x^2-2x-3\right)^2}$ nên $y'=\dfrac{2(x^2-2x-3)(2x-2)}{2\sqrt{\left(x^2-2x-3\right)^2}}=\dfrac{(x^2-2x-3)(2x-2)}{|x^2-2x-3|}$\\
			$y'=0 \Leftrightarrow \dfrac{(x^2-2x-3)(2x-2)}{|x^2-2x-3|}=0 \Leftrightarrow x=1$\\
			\centerline{(hàm số không có đạo hàm tại $x=-1$ và $x=3$)}
			Bảng biến thiên:
			\begin{center}
				\begin{tikzpicture}
				\tkzTabInit[lgt=1,espcl=3]
				{$x$/0.7,$y'$/0.7,$y$/2}
				{$-\infty$,$-1$,$1$,$3$,$+\infty$}
				\tkzTabLine{,-,d,+,$0$,-,d,+,}
				\tkzTabVar{+/$+\infty$,-/$0$,+/$4$,-/$0$,+/$+\infty$}
				\end{tikzpicture}
			\end{center}
			Vậy hàm số nghịch biến trong các khoảng $(-\infty;-1)$ và $(1;3)$,\\ hàm số đồng biến trên các khoảng $(-1;1)$ và $(3;+\infty)$.
		}
	\end{bt}
	
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		\immini{Cho hàm số $y=f(x)$ liên tục trên $\mathbb{R}$. Hàm số $y=f'(x)$ có đồ thị như hình bên. Hãy xét sự đồng biến, nghịch biến của hàm số $y=f(2-x)$.}
		{\begin{tikzpicture}[line width=0.6pt,>=stealth,scale=0.8] 
			\clip(-1.5,-2.5) rectangle (4.9,2.7);
			\draw[->] (-1.5,0) -- (4.7,0) node[below] {$x$};
			\draw[->] (0,-2.5) -- (0,2.5) node[left] {$y$};
			\node at (0,0) [below left] {$O$};
			\node at (-1.3,0) [above]{$-1$};
			\node at (1.1,0) [above]{$1$};
			\node at (4.3,0) [above]{$4$};
			\node[rotate=78] at (4.3,1.2) [above]{$y=f'(x)$};
			\draw[smooth,samples=300,domain=-1.5:4.5] plot(\x,{(\x-1)*((\x)+1)*((\x)-4)/4});
			\end{tikzpicture}}
		\loigiai{
			Ta có ${\left(f(2-x)\right)}'=(2-x)'.f'(2-x)=-f'(2-x)$
			Dựa vào đồ thị hàm số $f'(x)$ thì
			\begin{itemize}
				\item ${\big(f(2-x)\big)}'>0 \Leftrightarrow f'(2-x)<0 \Leftrightarrow \left[\begin{aligned}
				&2-x<-1 \\
				&1<2-x<4
				\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
				&x>3 \\
				&-2<x<1
				\end{aligned}\right. $.\\
				\item ${\big(f(2-x)\big)}'<0 \Leftrightarrow f'(2-x)>0 \Leftrightarrow \left[\begin{aligned}
				&-1<2-x<1 \\
				&2-x>4
				\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
				&1<x<3 \\
				&x<-2
				\end{aligned}\right. $.
			\end{itemize}
			Vậy hàm số $f(x)$ đồng biến trên mỗi khoảng $(-2;1)$ và $(3;+\infty)$.\\
			Hàm số $f(x)$ nghịch biến trên mỗi khoảng $(-\infty;-2)$ và $(1;3)$.
		}
	\end{bt}
	\begin{dang}{Điều kiện của tham số để một hàm số đơn điệu trên mọi khoảng xác định}
		\begin{enumerate}[\bf A.]
			\item \textbf{Lý thuyết chung}\\
			Cho hàm số $y=f(x)$ liên tục trên $K$ (một khoảng, một đoạn hoặc nửa khoảng) đồng thời phương trình $f'(x)$ vô nghiệm trên $K$ hoặc có nghiệm rời rạc trên $K$. Khi đó
			\begin{itemize}
				\item Hàm số $f(x)$ đồng biến trên $K \Leftrightarrow f'(x) \geqslant 0, \forall x\in K$.
				\item Hàm số $f(x)$ nghịch biến trên $K \Leftrightarrow f'(x) \leq  0, \forall x\in K$.
			\end{itemize}
			\item \textbf{Kiến thức bổ trợ}\\
			Cho tam thức bậc hai $h(x)=ax^2+bx+c~(a \neq 0)$. Khi đó
			\begin{itemize} \begin{multicols}{2}
					\item $h(x) \geqslant 0, \forall x \in \mathbb{R} \Leftrightarrow \left\{\begin{aligned}& a>0\\& \Delta \leq  0. \end{aligned}\right.$
					\item $h(x) \leq  0, \forall x \in \mathbb{R} \Leftrightarrow \left\{\begin{aligned}& a<0\\& \Delta \leq  0. \end{aligned}\right.$
			\end{multicols}\end{itemize}
			\underline{Lưu ý}: khi đã chắc chắn $a \neq 0$, hai công thức trên đây mới được sử dụng.
		\end{enumerate}
	\end{dang}
	\begin{vd}%[Dương Phước Sang]%[2D1K1]
		Tìm các giá trị của tham số $m$ để hàm số $y=x^3-3x^2+3(m+2)x+3m-1$ đồng biến trên $\mathbb{R}$.
		\loigiai{
			Hàm số $y=x^3-3x^2+3(m+2)x+3m-1$ có tập xác định $\mathscr{D}=\mathbb{R}$.\\
			Hàm số đồng biến trên $\mathbb{R} \Leftrightarrow y'=3x^2-6x+3(m+2)\geqslant 0,\forall x\in \mathbb{R}$.\\
			\centerline{$\Leftrightarrow \left\{\begin{aligned}& a>0 \\ & \Delta'\leq  0 \\ \end{aligned} \Leftrightarrow \left\{\begin{aligned}& 3>0 \\ & 9-9(m+2)\leq  0 \\ \end{aligned} \Leftrightarrow m\geqslant -1\right. \right.$}
			Vậy với $m\geqslant -1$ thì hàm số đồng biến trên $\mathbb{R}$.
		}
	\end{vd}
	\begin{vd}%[Dương Phước Sang]%[2D1K1]
		Tìm các giá trị của tham số $m$ để hàm số $y=\dfrac{1}{3}(3-m)x^3-(m+3)x^2+(m+2)x-3$  đồng biến trên $\mathbb{R}$.
		\loigiai{
			Hàm số $y=\dfrac{1}{3}(3-m)x^3-(m+3)x^2+(m+2)x-3$ có tập xác định $\mathscr{D}=\mathbb{R}$.
			\begin{itemize}
				\item Xét $a=3-m=0 \Leftrightarrow m=3$.\\
				Khi đó hàm số trở thành $y=-6x^2+5x-3$. Đây là hàm số bậc hai, có lúc tăng, lúc giảm khi xét trên $\mathbb{R}$. Do đó ta loại $m=3$.
				\item Xét $a=3-m\ne 0 \Leftrightarrow m\ne 3$.\\
				Hàm số luôn tăng trên $\mathbb{R} \Leftrightarrow y'=(3-m)x^2-2(m+3)x+(m+2)\geqslant 0$\\
				\centerline{$ \Leftrightarrow \left\{\begin{aligned}
					&a=3-m>0 \\
					&\Delta'=2m^2+5m+3\leq  0
					\end{aligned}\right. \Leftrightarrow \left\{\begin{aligned}
					&m<3 \\
					&-\dfrac{3}{2}\leq  m\leq  -1
					\end{aligned}\right. \Leftrightarrow -\dfrac{3}{2}\leq  m\leq  -1$.}
			\end{itemize}
			Vậy với $-\dfrac{3}{2}\leq  m\leq  -1$ thì hàm số đồng biến trên $\mathbb{R}$.
		}
	\end{vd}
	\begin{vd}%[Dương Phước Sang]%[2D1B1]
		Tìm các giá trị của tham số $m$ để hàm số $y=\dfrac{mx+m-7}{5x-m+3}$ đồng biến trên mọi khoảng của tập xác định.
		\loigiai{
			Tập xác định: $\mathscr{D}=\mathbb{R}\setminus \left\{\dfrac{m-3}{5}\right\}$.\\ Ta có $y'=\dfrac{-m^2-2m+35}{(5x-m+3)^2}$.\\
			Hàm số đồng biến trên mọi khoảng xác định khi và chỉ khi\\
			\centerline{$y'>0,\forall x\ne \dfrac{m-3}{5} \Leftrightarrow -m^2-2m+35>0 \Leftrightarrow m\in (-7;5)$.}
			Vậy, với $m\in (-7;5)$ thì hàm số đồng biến trên mọi khoảng xác định của nó.
		}
	\end{vd}
	\begin{center}
		\textbf{BÀI TẬP TỰ LUYỆN}
	\end{center}
	
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các giá trị của tham số $m$ để hàm số $y=\dfrac{1}{3}x^3+mx^2+4x+3$ đồng biến tren $\mathbb{R}$.
		\loigiai{
			Tập xác định: $\mathscr{D}=\mathbb{R}$.\\
			Đạo hàm: $y'=3x^2+2mx+4$ có $\Delta'_{y'}=m^2-12$.\\
			Hàm số đồng biến trên $\mathbb{R} \Leftrightarrow y'\geqslant 0,\forall x\in \mathbb{R} \Leftrightarrow \left\{\begin{aligned}
			&a>0 \\
			&\Delta'_{y'}\leq  0
			\end{aligned}\right. \Leftrightarrow \left\{\begin{aligned}
			&3>0: \text{hiển nhiên} \\
			&m^2-12\leq  0
			\end{aligned}\right. \Leftrightarrow |m|\leq  2\sqrt{3}$.\\
			Vậy, với $m\in \left[-2\sqrt{3};2\sqrt{3}\right]$ thì hàm số đồng biến trên $\mathbb{R}$.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các giá trị của tham số $m$ để hàm số $y=-x^3+3x^2+3(m^2-1)x-3m^2-1$ nghịch biến trên $\mathbb{R}$.
		\loigiai{
			Hàm số $y=-x^3+3x^2+3(m^2-1)x-3m^2-1$ có tập xác định $\mathscr{D}=\mathbb{R}$.\\
			Hàm số luôn giảm trên $\mathbb{R} \Leftrightarrow y'=-3x^2+6x+3(m^2-1)\leq  0,\forall x\in \mathbb{R}$\\
			\centerline{$ \Leftrightarrow \left\{\begin{aligned}
				&a=-3<0 \\
				&\Delta'=9+3.3(m^2-1)=9m^2\leq  0
				\end{aligned}\right. \Leftrightarrow m=0$.}
			Vậy với $m=0$ thì hàm số nghịch biến trên $\mathbb{R}$.
		}
	\end{bt}
	
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các giá trị của tham số $m$ để hàm số $y=\dfrac{mx-2}{x-m+1}$ nghịch biến trên từng khoảng xác định của nó.
		\loigiai{
			Hàm số $y=\dfrac{mx-2}{x-m+1}$ có tập xác định  $\mathscr{D}=\mathbb{R}\setminus \{m-1\}$.\\
			Hàm số nghịch biến trên từng khoảng xác định $\Leftrightarrow y'=\dfrac{-m^2+m+2}{(x-m+1)^2}<0,\forall x\ne m-1$\\
			\centerline{$\Leftrightarrow -m^2+m+2<0 \Leftrightarrow \left[\begin{aligned}
				&m<-1 \\
				&m>2.
				\end{aligned}\right.$}
			Vậy với $m\in (-\infty;-1)\cup (2;+\infty)$ thì hàm số nghịch biến trên từng khoảng xác định.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các giá trị của tham số $m$ để hàm số $y=x+m\cos x$ đồng biến trên $\mathbb{R}$.
		\loigiai{
			Hàm số $y=x+m\cos x$ có tập xác định $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=1-m\sin x$.\\
			Hàm số đồng biến trên $\mathbb{R} \Leftrightarrow y'\geqslant 0,\forall x\in \mathbb{R} \Leftrightarrow 1-m\sin x\geqslant 0,\forall x\in \mathbb{R} \Leftrightarrow m\sin x\leq  1,\forall x\in \mathbb{R}$ (*)\\
			Với $m=0$ thì (*) luôn đúng.\\
			Với $m>0$ thì (*) $\Leftrightarrow \sin x\leq  \dfrac{1}{m},\forall x\in \mathbb{R} \Leftrightarrow 1\leq  \dfrac{1}{m} \Leftrightarrow 0<m\leq  1$.\\
			Với $m<0$ thì (*) $\Leftrightarrow \sin x\geqslant \dfrac{1}{m},\forall x\in \mathbb{R} \Leftrightarrow -1\geqslant \dfrac{1}{m} \Leftrightarrow -1\leq  m<0$.\\
			Vậy $-1\leq  m\leq  1$ là các giá trị của tham số $m$ thỏa yêu cầu của bài toán.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Cho hàm số $y=\dfrac{(m+1)x^2-2mx+6m}{x-1}$. Tìm các giá trị của tham số m để hàm số đồng biến trên mọi khoảng của tập xác định hàm số.
		\loigiai{
			Tập xác định của hàm số: $\mathscr{D}=\mathbb{R} \setminus \{1\}$.\\
			Ta cần xét hai trường hợp
			\begin{itemize}
				\item \textbf{\underline{TH1}:} Khi $m=-1$, ta có hàm số $y=\dfrac{2x-6}{x-1}$ và $y'=\dfrac{4}{(x-1)^2}>0$ với mọi $ x\in \mathscr{D}$.\\
				Do đó hàm số đồng biến trên mỗi khoảng xác định.
				Vậy, $m=-1$ thỏa yêu cầu bài toán.
				\item \textbf{\underline{TH2}:} Khi $m\ne -1$, ta có $y'=\dfrac{(m+1)x^2-2(m+1)x-4m}{(x-1)^2}$.\\
				Đặt $g(x)=(m+1)x^2-2(m+1)x-4m$ và ta có $y'$ cùng dấu với $g(x)$.\\
				Hàm số đồng biến trên mỗi khoảng xác định $ \Leftrightarrow y'\geqslant 0,\forall x\in \mathscr{D} \Leftrightarrow g(x)\geqslant 0,\forall x\in \mathscr{D}$\\
				$\Leftrightarrow \left\{\begin{aligned}
				& \Delta_g'=(m+1)^2+4m(m+1)\leq  0 \\
				&m+1>0
				\end{aligned}\right. \Leftrightarrow \left\{\begin{aligned}
				&(m+1)(5m+1)\leq  0 \\
				&m>-1
				\end{aligned}\right. \Leftrightarrow -1<m\leq  -\dfrac{1}{5}$.\\
				Vậy tập hợp các giá trị của tham số $m$ thỏa yêu cầu của bài toán là $\left[-1;-\dfrac{1}{5}\right]$.
			\end{itemize}
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các giá trị của tham số $m$ để hàm số  $y=(m+2)\dfrac{x^3}{3}-(m+2)x^2-(3m-1)x+m^2$ đồng biến trên $\mathbb{R}$.
		\loigiai{
			Tập xác định của hàm số là $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=(m+2)x^2-2(m+2)x-3m+1$.\\
			Vì đạo hàm không thể triệt tiêu tại vô hạn điểm nên hàm số đồng biến trên $\mathbb{R}$ khi và chỉ khi\\
			\centerline{$y'\geqslant 0,\forall x\in \mathbb{R}$
				$ \Leftrightarrow (m+2)x^2-2(m+2)x-3m+1\geqslant 0,\forall x\in \mathbb{R}$ (1)}
			\begin{itemize}
				\item \underline{TH1}: Nếu $m=-2$ khi đó $(1)$ luôn đúng với mọi $x \Rightarrow m=-2$ thỏa bài toán.
				\item \underline{TH2}: Nếu $m\ne -2$ khi đó (1) thỏa với mọi $x\in \mathbb{R} \Leftrightarrow \left\{\begin{aligned}
				&a=m+2>0 \\
				& \Delta '=(m+2)(4m+1)\leq  0
				\end{aligned}\right. $\\
				\centerline{$ \Leftrightarrow \left\{\begin{aligned}
					&m+2>0 \\
					&4m+1\leq  0
					\end{aligned}\right. \Leftrightarrow -2<m\leq  -\dfrac{1}{4}$.}
			\end{itemize}
			Kết hợp cả hai trường hợp, ta có $-2\leq  m\leq  -\dfrac{1}{4}$ là những giá trị cần tìm.
		}
	\end{bt}
	\begin{bt}%[Dương Phước Sang]%[2D1K1]
		Tìm các giá trị của tham số $m$ để hàm số  $y=\dfrac{1}{3}(m^2-1)x^3+(m+1)x^2+3x$ luôn đồng biến trên $\mathbb{R}$.
		\loigiai{
			Tập xác định của hàm số là $\mathscr{D}=\mathbb{R}$.\\
			Ta có $y'=(m^2-1)x^2+2(m+1)x+3$ và có $ \Delta '=2(-m^2+m+2)$.\\
			Hàm số $y$ đồng biến trên $\mathbb{R}$ khi và chỉ khi $ \Leftrightarrow y'\geqslant 0,\forall x\in \mathbb{R}$.
			\begin{itemize}
				\item Xét $m^2-1=0 \Leftrightarrow m=\pm 1$.
				\begin{itemize}
					\item[+] $m=1 \Rightarrow y'=4x+3$. Ta có $y'\geqslant 0 \Leftrightarrow x\geqslant -\dfrac{3}{4} \Rightarrow m=1$ không thoả yêu cầu.
					\item[+] $m=1 \Rightarrow y'=3>0,\forall x\in \mathbb{R} \Rightarrow m=-1$ thoả mãn yêu cầu bài toán.
				\end{itemize}
				\item Xét $m^2-1\ne 0 \Leftrightarrow m\ne \pm 1$.\\
				Trường hợp này, hàm số đồng biến trên $\mathbb{R}$ khi và chỉ khi\\
				\centerline{$\left\{\begin{aligned}& a=m^2-1>0\\& \Delta' = 2(-m^2+m+2) \leq  0 \end{aligned}\right. \Leftrightarrow \left[\begin{aligned}& m<-1\\&m\geqslant 2. \end{aligned}\right.$}
			\end{itemize}
			Vậy với $m \leq  -1$ hoặc $m \geqslant 2$ thì hàm số $y$ đồng biến trên $\mathbb{R}$.
		}
	\end{bt}

\begin{dang}{Tìm các khoảng đơn điệu; chứng minh hàm số đơn điệu trên tập $\mathscr{K}$ }
	{\bf Phương pháp}
	\begin{itemize}
		\item \underline{Bước 1:} Tìm tập xác định của hàm số hoặc xét hàm số trên tập $\mathscr{K}$.
		\item \underline{Bước 2:} Tính đạo hàm $y^\prime=f^\prime (x)$.
		\item \underline{Bước 3:} Xét dấu $f^\prime (x)$.
		\item \underline{Bước 4:} Kết luận.
	\end{itemize}
\end{dang}
\begin{vd}%[2D1B1]
	Tìm các khoảng đơn điệu của hàm số
	$y=\sqrt{x+1}+\sqrt{5-x}$.
	\loigiai{
		Tập xác định của hàm số $\mathscr{D}=[-1;5]$.\\
		$y'=\dfrac{1}{2\sqrt{x+1}}-\dfrac{1}{2\sqrt{5-x}}=\dfrac{\sqrt{5-x}-\sqrt{x+1}}{2\sqrt{(x+1)(5-x)}}$;\\
		Cho $y'=0\Leftrightarrow x=2$.\\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /.7,$y'$ /.7,$y$ /2}
			{$-1$,$2$,$5$}
			\tkzTabLine{,+,$0$,-,}
			\tkzTabVar{-/$2$,+/$2\sqrt{3}$ ,-/$\sqrt{6}$}
			\end{tikzpicture}
		\end{center}
		Vậy hàm số đồng biến trên khoảng $(-1;2)$ và nghịch biến trên khoảng $(2;5)$.
	}
\end{vd}
\begin{vd}%[2D1B1]
	Xét chiều biến thiên của hàm số $y=2x-1-\sqrt{3x-5}.$
	\loigiai{
		Tập xác định của hàm số $\mathscr{D}=\left[\dfrac{5}{3};+\infty \right)$.\\
		Ta có $y'=2-\dfrac{3}{2\sqrt{3x-5}}=\dfrac{4\sqrt{3x-5}-3}{2\sqrt{3x-5}}$;\\
		Cho $y'=0\Leftrightarrow x=\dfrac{89}{48}$.\\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /1,$y'$ /.7,$y$ /2}
			{$\dfrac{5}{3}$,$\dfrac{89}{48}$,$+\infty$}
			\tkzTabLine{,-,$0$,+,}
			\tkzTabVar{+/$\dfrac{7}{3}$,-/$\dfrac{47}{24}$ ,+/$+\infty$}
			\end{tikzpicture}
		\end{center}
		Vậy hàm số nghịch biến trên khoảng $\left(\dfrac{5}{3};\dfrac{89}{48}\right)$ và đồng biến trên khoảng $\left(\dfrac{89}{48};+\infty \right)$}.
\end{vd}
\begin{vd}%[2D1B1]
	Chứng minh rằng hàm số $y=\sqrt{4x-x^2} $ đồng biến trên đoạn $[0;2]$.
	\loigiai{Hàm số $y=\sqrt{4x-x^2} $ liên tục trên đoạn $[0;2]$.\\
		Ta có $y^\prime =\dfrac{2-x}{\sqrt{4x-x^2}}>0\,\,\, \forall x\in [0;2]$.\\
		Do đó hàm số đồng biến trên đoạn $[0;2]$.
		
	}
\end{vd}
\begin{vd}%[2D1B1]
	Chứng minh hàm số $y=\sqrt{x^2-1}$ nghịch biến trên nửa khoảng $(-\infty;-1]$.
	\loigiai{Hàm số $y=\sqrt{x^2-1}$ liên tục trên nửa khoảng $(-\infty;-1]$.\\
		Ta có $y^\prime =\dfrac{x}{\sqrt{x^2-1}}<0\,\,\, \forall x\in (-\infty;-1)$.\\
		Do đó hàm số nghịch biến trên nửa khoảng $(-\infty;-1]$.
		
	}
\end{vd}
\begin{vd}%[2D1K1]
	Tìm các khoảng đơn điệu của hàm số $y=\cos2x+4\cos x$ trên đoạn $[0;2\pi]$.
	\loigiai{Hàm số $y=\cos2x+4\cos x$ liên tục trên đoạn $[0;2\pi]$.\\
		Ta có $y^\prime =-2\sin2x-4\sin x=-4\sin x(\cos x+1)$.\\
		Trên đoạn $[0;2\pi]$, $y^\prime =0$ có nghiệm $x=0$, $x=\pi$, $x=2\pi$.\\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /.7,$y'$ /.7,$y$ /2}
			{$0$,$\pi$,$2\pi$}
			\tkzTabLine{$0$,-,$0$,+,$0$}
			\tkzTabVar{+/$5$,-/$-3$ ,+/$5$}
			\end{tikzpicture}
		\end{center}
		Do đó hàm số nghịch biến trên  khoảng $(0;\pi)$ và đồng biến trên khoảng $(\pi; 2\pi)$.
		
	}
\end{vd}
\begin{center}
	\textbf{BÀI TẬP TỰ LUYỆN}
\end{center}


\begin{bt}%[2D1B1]
	Xét chiều biến thiên của hàm số $y=\sqrt{x+2}+\sqrt{2-x}$.
	\loigiai{
		Tập xác định của hàm số $\mathscr{D}=[-2;2]$.\\
		Ta có $y^\prime =\dfrac{\sqrt{2-x}-\sqrt{x+2}}{2\sqrt{4-x^2}}$. Cho $y^\prime =0 \Leftrightarrow x=0$.\\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /.7,$y'$ /.7,$y$ /2}
			{$-2$,$0$,$2$}
			\tkzTabLine{,+,$0$,-,}
			\tkzTabVar{-/$2$,+/$2\sqrt{2}$ ,-/$2$}
			\end{tikzpicture}
		\end{center}
		Vậy hàm số đồng biến trên khoảng $(-2;0)$ và nghịch biến trên khoảng $(0;2)$.
	}
\end{bt}
\begin{bt}%[2D1B1]
	Xét chiều biến thiên của hàm số $y=x+\sqrt{1-x^2}$.
	\loigiai{
		Tập xác định của hàm số $\mathscr{D}=[-1;1]$.\\
		Ta có $y^\prime =1-\dfrac{x}{\sqrt{1-x^2}}=\dfrac{\sqrt{1-x^2}-x}{\sqrt{1-x^2}}$. Cho $y^\prime =0 \Leftrightarrow \sqrt{1-x^2}=x\Leftrightarrow x=\dfrac{1}{\sqrt{2}}$.\\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /1,$y'$ /.7,$y$ /2}
			{$-1$,$\dfrac{1}{\sqrt{2}}$,$1$}
			\tkzTabLine{,+,$0$,-,}
			\tkzTabVar{-/$-1$,+/$\sqrt{2}$ ,-/$1$}
			\end{tikzpicture}
		\end{center}
		Vậy hàm số đồng biến trên khoảng $\left(-1;\dfrac{1}{\sqrt{2}}\right)$ và nghịch biến trên khoảng $\left(\dfrac{1}{\sqrt{2}};1\right)$.
	}
\end{bt}
\begin{bt}%[2D1B1]
	Chứng minh hàm số $y=\sqrt{x^2-25}$ đồng biến trên khoảng $(5;+\infty)$.
	\loigiai{
		Hàm số liên tục trên khoảng $(5;+\infty)$.\\
		Ta có $y^\prime =\dfrac{x}{2\sqrt{x^2-25}}>0\,\,\, \forall x\in (5;+\infty)$.\\
		Do đó hàm số đồng biến trên khoảng $(5;+\infty)$.
	}
\end{bt}

\begin{bt}%[2D1K1]
	Tìm các khoảng đơn điệu của hàm số $y=\dfrac{x}{2}+\cos x$ trên đoạn $[0;\pi]$.
	\loigiai{Hàm số $y=\dfrac{x}{2}+\cos x$ trên đoạn $[0;\pi]$.\\
		Ta có $y^\prime =\dfrac{1}{2}-\sin x$.\\
		Trên đoạn $[0;\pi]$, $y^\prime =0$ có nghiệm $x=0$, $x=\dfrac{\pi}{6}$, $\dfrac{5\pi}{6}$.\\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /1,$y'$ /.7,$y$ /3}
			{$0$,$\dfrac{\pi}{6}$,$\dfrac{5\pi}{6}$,$\pi$}
			\tkzTabLine{,+,$0$,-,$0$,+}
			\tkzTabVar{-/$1$,+/$\dfrac{\pi}{12}+\dfrac{\sqrt{3}}{2}$,-/$\dfrac{5\pi}{12} -\dfrac{\sqrt{3}}{2}$,+/$\dfrac{\pi}{2}-1$}
			\end{tikzpicture}
		\end{center}
		Do đó hàm số nghịch biến trên các  khoảng $\left(0;\dfrac{\pi}{6}\right)$ và $\left(\dfrac{5\pi}{6};\pi\right)$; hàm số nghịch biến trên khoảng $\left(\dfrac{\pi}{6};\dfrac{5\pi}{6}\right)$.
		
	}
\end{bt}
\begin{dang}{Tìm điều kiện của tham số để hàm số đơn điệu trên một khoảng cho trước}
	Có hai phương pháp chính để giải các bài toán.
	\begin{itemize}
		\item \textbf{Phương pháp 1:} Cô lập tham số, lập bảng biến thiên, từ đó rút ra điều kiện của tham số.
		
		\item \textbf{Phương pháp 2:} Lập bảng biến thiên trực tiếp để tìm các khoảng đơn điệu cụ thể, từ đó rút ra kết luận.
	\end{itemize}
\end{dang}
\begin{vd}%[2D1K1]
	Tìm $m$ để hàm số $y = -x^3+3x^2+3mx-1$ nghịch biến trên $\left( {0;+\infty} \right)$.
	\loigiai{
		Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có $y'=-3x^2+6x+3m$. 
		
		Hàm số nghịch biến trên $\left( {0;+\infty} \right)$ khi và chỉ khi  $y'\leq  0,\forall x\in \left( {0;+\infty} \right)$.
		
		Hay $-3x^2+6x+3m\leq  0,\forall x\in \left( {0;+\infty} \right)\Leftrightarrow m\leq  x^2-2x, \forall x\in \left( {0;+\infty} \right)\ (1)$. 
		
		Xét hàm số $f(x)=x^2-2x$ trên $\left( {0;+\infty} \right)$ có $f'(x)=2x-2; f'(x)=0\Leftrightarrow x=1$.\\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /.7,$f'(x)$ /.7,$f(x)$ /2}
			{$0$,$1$,$+\infty$}
			\tkzTabLine{,-,$0$,+,}
			\tkzTabVar{+/ $0$ ,-/$-1$,+/$+\infty$}
			\end{tikzpicture}
		\end{center}
		Từ bảng biên thiên ta có $(1)\Leftrightarrow m\leq  -1$.
		
		Vậy với $m\leq  -1$ thì hàm số đã cho nghịch biến trên $\left( {0;+\infty} \right)$.
		
	}
\end{vd}
\begin{vd}%[2D1K1]
	Tìm $m$ để hàm số $y = - \dfrac{1}{3}{x^3} + \left( {m - 1} \right){x^2} + \left( {m + 3} \right)x + 4$ đồng biến trên $\left( {0;3} \right)$.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có $y' = - {x^2} + \left( {m - 1} \right)x + m + 3$. 
		
		Hàm số đồng biến trên $\left( {0;3} \right) $ khi và chỉ khi $y' \geqslant 0,\forall x \in \left( {0;3} \right)$.
		
		Hay $- {x^2} + 2\left( {m - 1} \right)x + m + 3 \geqslant 0,\forall x \in \left( {0;3} \right) \Leftrightarrow m\left( {2x + 1} \right) \geqslant {x^2} + 2x - 3,\forall x \in \left( {0;3} \right) $.
		
		Trên $(0;3)$ ta có $2x+1>0$ nên chia hai vế cho $2x+1$ được $m \geqslant \dfrac{{{x^2} + 2x - 3}}{{2x + 1}},\forall x \in \left( {0;3} \right)\ (2)$.
		
		Xét hàm số $f(x) = \dfrac{{{x^2} + 2x - 3}}{{2x + 1}}$ trên $\left[ {0;3} \right]$ có $\displaystyle f'(x) = \frac{{2{x^2} + 2x + 8}}{{{{\left( {2x + 1} \right)}^2}}} > 0,\forall x \in \left[ {0;3} \right]$.
		
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /1,$f'(x)$ /.7,$f(x)$ /1.5}
			{$0$,$3$}
			\tkzTabLine{,+,}
			\tkzTabVar{-/$-3$,+/$\dfrac{12}{7}$}
			\end{tikzpicture}
		\end{center}
		
		Từ bảng biến thiên ta có $(2) \Leftrightarrow m \geqslant \dfrac{{12}}{7}$.
		
		Vậy với $m \geqslant \dfrac{{12}}{7}$, hàm số đã cho luôn đồng biến trên $\left( {0;3} \right)$.
	}
\end{vd}
\begin{vd}%[2D1K1]
	Tìm $m$ để hàm số $y = {x^3} - \left( {2m + 1} \right){x^2} + \left( {{m^2} + 2m} \right)x + 1$ đồng biến trên $\left( {0; + \infty } \right)$.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có: $y' = 3{x^2} - 2\left( {2m + 1} \right)x + {m^2} + 2m$; $\Delta '_{y'} = {\left( {2m + 1} \right)^2} - 3\left( {{m^2} + 2m} \right) = {\left( {m - 1} \right)^2}$.
		
		Với $m = 1$, ta có $y' \geqslant 0,\forall x \in \mathbb{R} \Rightarrow $ hàm số luôn đồng biến trên $\mathbb{R}$ nên đồng biến trên $\left( {0; + \infty } \right)$.
		
		Do đó $m=1$ thỏa mãn yêu cầu bài toán.
		
		Với $m \ne 1$, ta có $\displaystyle y' = 0 \Leftrightarrow \left[ \begin{array}{l}
		{x_1} = \dfrac{{2m + 1 - \left| {m - 1} \right|}}{3}\\
		{x_2} = \dfrac{{2m + 1 + \left| {m - 1} \right|}}{3}
		\end{array} \right.$.
		
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /1,$f'(x)$ /.7,$f(x)$ /1.5}
			{$-\infty$,$x_1$,$x_2$,$+\infty$}
			\tkzTabLine{,+,$0$,-,$0$,+}
			\tkzTabVar{-/$-\infty$,+/$y_1$,-/$y_2$,+/$+\infty$}
			\end{tikzpicture}
		\end{center}
		
		
		Từ bảng biến thiên suy ra hàm số đồng biến trên $\left( {0; + \infty } \right) $ khi và chỉ khi $x_2\leq  0$.
		
		Hay $\dfrac{{2m + 1 + \left| {m - 1} \right|}}{3} \leq  0 \Leftrightarrow \left| {m - 1} \right| \leq  - 2m - 1$.
		
		Với $m > 1$, ta có $\left| {m - 1} \right| \leq  - 2m - 1 \Leftrightarrow m - 1 \leq  - 2m - 1 \Leftrightarrow m < 0 \text{ (loại)}  $.
		
		Với $m < 1$, ta có $\left| {m - 1} \right| \leq  - 2m - 1 \Leftrightarrow - m + 1 \leq  - 2m - 1 \Leftrightarrow m < - 2\text{ (thỏa mãn)} $.
		
		Vậy với $m \leq  - 2$ hoặc $m = 1$, hàm số đã cho đồng biến trên $\left( {0; + \infty } \right)$.  
		
	}
\end{vd}
\begin{vd}%[2D1K1]
	Tìm $m$ để hàm số $y = \dfrac{{{x^2} - 2mx + 2{m^2} - 2}}{{x - m}}$ đồng biến trên $\left( {1; + \infty } \right)$.
	\loigiai{
		Tập xác định $\mathscr{D} = \mathbb{R}\backslash \left\{ m \right\}$. Ta có $\displaystyle y' = \frac{{{x^2} - 2mx + 2}}{{{{\left( {x - m} \right)}^2}}}$.
		
		Hàm số đồng biến trên $\left( {1; + \infty } \right) $ khi và chỉ khi $\begin{cases}y' \geqslant 0,\forall x \in \left( {1; + \infty } \right)\\m \notin \left( {1; + \infty } \right)\end{cases}$.
		
		Hay $m\leq  1$ và ${x^2} - 2mx + 2 \geqslant 0,\forall x \in \left( {1; + \infty } \right) \Leftrightarrow m \leq  \dfrac{{{x^2} + 2}}{{2x}},\forall x \in \left( {1; + \infty } \right)\ (4)$.
		
		Xét hàm số $\displaystyle f(x) = \frac{{{x^2} + 2}}{{2x}}$ trên $\left[ {1; + \infty } \right)$ có $f'(x) = \dfrac{{2{x^2} - 4}}{{4{x^2}}};f'(x) = 0 \Leftrightarrow x = \sqrt 2 $.
		
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /1,$f'(x)$ /.7,$f(x)$ /2}
			{$1$,$\sqrt{2}$,$+\infty$}
			\tkzTabLine{,-,$0$,+}
			\tkzTabVar{+/$\dfrac{3}{2}$,-/$\sqrt{2}$,+/$+\infty$}
			\end{tikzpicture}
		\end{center}
		
		
		Từ bảng biến thiên ta có $(4) \Leftrightarrow \begin{cases}
		m \le \sqrt{2}\\
		m \le 1
		\end{cases} $ $\Leftrightarrow m\le 1$.
		
		Vậy với $m \leq  1$, hàm số đã cho đồng biến trên $\left( {1; + \infty } \right)$.
	}
\end{vd}
\begin{center}
	\textbf{BÀI TẬP TỰ LUYỆN}
\end{center}
\begin{bt}%[2D1K1]
	Tìm $m$ để hàm số $y = {x^3} + 3{x^2} - mx - 4$ nghịch biến trên $\left( { - \infty ;0} \right)$.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có $y'=3x^2+6x-m$.
		
		Hàm số đồng biến trên $\left( { - \infty ;0} \right)$ khi và chỉ khi  $y'\geqslant 0,\forall x\in \left( -\infty;0 \right)$.
		
		Hay $3x^2+6x-m \geqslant 0,\forall x\in (-\infty;0) \Leftrightarrow m \leq  3x^2+6x,\forall x\in (-\infty;0)\ (1)$. 
		
		Xét hàm số $f(x)=3x^2+6x$ trên $(-\infty; 0]$ có $f'(x)=6x+6; f'(x)=0\Leftrightarrow x=-1$. 
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /1,$f'(x)$ /.7,$f(x)$ /1.5}
			{$-\infty$,$-1$,$0$}
			\tkzTabLine{,-,$0$,+}
			\tkzTabVar{+/$+\infty$,-/$-3$,+/$0$}
			\end{tikzpicture}
		\end{center}
		Từ bảng biên thiên ta có $(1)\Leftrightarrow m\leq  -3$.
		
		Vậy với $m\leq  -3$ thì hàm số đã cho đồng biến trên $\left( -\infty;0 \right)$.
		
	}
\end{bt}

\begin{bt}%[2D1K1]
	Tìm $m$ để hàm số $ y = \dfrac{1}{3}m{x^3} - \left( {m - 1} \right){x^2} + 3\left( {m - 2} \right)x + \dfrac{1}{3}$ đồng biến trên $\left[ {2; + \infty } \right)$.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có $y'=mx^2-2(m-1)x+3(m-2)=m\left(x^2-2x+3\right)+2x-6$.
		
		Hàm số đồng biến trên $\left[ 2;+\infty \right)$ khi và chỉ khi  $y'\geqslant 0,\forall x\in \left[2;+\infty \right)$.
		
		Hay $m\left(x^2-2x+3\right)+2x-6 \geqslant 0,\forall x\in [2;+\infty) \Leftrightarrow m \geqslant \dfrac{-2x+6}{x^2-2x+3},\forall x\in [2;+\infty)\ (2)$. 
		
		Xét hàm số $f(x)=\dfrac{-2x+6}{x^2-2x+3}$ trên $[2;+\infty)$ có $f'(x)=\dfrac{2x^2-12x+6}{\left(x^2-2x+6\right)^2}; f'(x)=0\Leftrightarrow x=3\pm \sqrt{6}$. 
		
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /1,$f'(x)$ /.7,$f(x)$ /2.5}
			{$2$,$3+\sqrt{6}$,$+\infty$}
			\tkzTabLine{,-,$0$,+}
			\tkzTabVar{+/$\dfrac{2}{3}$,-/$\dfrac{2-\sqrt{6}}{2}$,+/$0$}
			\end{tikzpicture}
		\end{center}
		
		Từ bảng biên thiên ta có $(2)\Leftrightarrow m\geqslant \dfrac{2}{3}$.
		
		Vậy với $m\geqslant \dfrac{2}{3}$ thì hàm số đã cho đồng biến trên $\left[2;+\infty \right)$.
	}
\end{bt}

\begin{bt}%[2D1K1]
	Tìm $m$ để hàm số $y = {x^4} - 8m{x^2} + 9m$ đồng biến trên $\left( {2; + \infty } \right)$.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có $y'=4x^3-16mx=4x\left(x^2-4m\right) $.
		
		Hàm số đồng biến trên $\left( 2;+\infty \right)$ khi và chỉ khi  $y'\geqslant 0,\forall x\in \left(2;+\infty \right)$.
		
		Hay $4x\left(x^2-4m\right) \geqslant 0,\forall x\in (2;+\infty) \Leftrightarrow m \leq  \dfrac{x^2}{4},\forall x\in (2;+\infty)\ (3)$. 
		
		Xét hàm số $f(x)=\dfrac{x^2}{4}$ trên $[2;+\infty)$ có $f'(x)=\dfrac{x}{2}>0,\forall x\in (2;+\infty)$. 
		
		Do đó $(3)\Leftrightarrow m\leq  f(2)\Leftrightarrow m\leq  1$.
		
		Vậy với $m\leq  1$ thì hàm số đã cho đồng biến trên $\left(2;+\infty \right)$.
		
	}
\end{bt}

\begin{bt}%[2D1K1]
	Tìm $m$ để hàm số $\displaystyle y = \frac{{mx + 4}}{{x + m}}$ nghịch biến trên $\left( { - \infty ;1} \right)$.
	\loigiai{
		Tập xác định $\mathscr{D} = \mathbb{R} \backslash \left\{ -m \right\}$. Ta có $\displaystyle y'= \frac{m^2-4}{(x+m)^2}$.
		
		Hàm số nghịch biến trên $\left( { - \infty ;1} \right) $ khi và chỉ khi $ y'<0, \forall x \in (-\infty; 1)$.
		
		Hay $\left\{ \begin{array}{l}
		- m \notin \left( { - \infty ;1} \right)\\
		{m^2} - 4 < 0
		\end{array} \right. \Leftrightarrow \left\{ \begin{array}{l}
		- m \geqslant 1\\
		- 2 < m < 2
		\end{array} \right. \Leftrightarrow  - 2 < m \leq   - 1$.
		
		Vậy với $m \in \left( { - 2; - 1} \right]$, hàm số đã cho luôn đồng biến trên mỗi khoảng xác định.
	}
\end{bt}

\begin{bt}%[2D1K1]
	Tìm $m$ để hàm số $\displaystyle y = \frac{{m{x^2} + 6x - 2}}{{x + 2}}$ nghịch biến trên $\left[ {1; + \infty } \right)$.
	\loigiai{
		Hàm số xác định và liên tục trên $\left[ {1; + \infty } \right)$. Ta có $y'=\dfrac{mx^2+4mx+14}{(x+2)^2}=\dfrac{m(x^2+4x)+14}{(x+2)^2}$.
		
		Hàm số nghịch biến trên $\left[ {1; + \infty } \right) $ khi và chỉ khi $y'\leq  0,\forall x\in [1;+\infty)$.
		
		Hay $\dfrac{m(x^2+4x)+14}{(x+2)^2}\leq  0,\forall x\in [1;+\infty) \Leftrightarrow m \leq  \dfrac{-14}{x^2+4x},\forall x\in [1;+\infty)\ (5)$.
		
		Xét hàm số $f(x)=\dfrac{-14}{x^2+4x}$ trên $[1;+\infty)$ có $f'(x)=\dfrac{28x+56}{(x^2+4x)^2}>0,\forall x\in [1;+\infty)$. 
		
		Do đó $(5) \Leftrightarrow m\leq  f(1)\Leftrightarrow m\leq  -\dfrac{14}{5}$. 
		
		Vậy với $m\leq  -\dfrac{14}{5}$, hàm số đã cho luôn nghịch biến trên $\left[ {1; + \infty } \right) $.
	}
\end{bt}

\begin{bt}%[2D1K1]
	Tìm $a$ để hàm số $\displaystyle y = \frac{{{x^2} - 2ax + 4{a^2}}}{{x - 2a}}$ đồng biến trên $\left( {2; + \infty } \right)$.
	\loigiai{
		Tập xác định $\mathscr{D} = \mathbb{R}\backslash \left\{ 2a \right\}$. Ta có $\displaystyle y' = \frac{{{x^2} - 4ax}}{{{{\left( {x - 2a} \right)}^2}}}$.
		
		Hàm số đồng biến trên $\left( {2; + \infty } \right) \Leftrightarrow y' \ge 0,\forall x \in \left( {2; + \infty } \right)$.
		
		Hay $\begin{cases}2a\notin (2;+\infty)\\x^2-4ax\geqslant 0,\forall x \in (2;+\infty)\end{cases}\Leftrightarrow \begin{cases}2a\leq  2\\a\leq  \dfrac{x}{4},\forall x\in (2;+\infty)\end{cases}\Leftrightarrow \begin{cases}
		a\leq  1\\a\leq  \dfrac{1}{2}\end{cases}\Leftrightarrow a\leq  \dfrac{1}{2}$.
		
		Vậy với $m\leq  \dfrac{1}{2}$, hàm số đồng biến trên $\left( {2; + \infty } \right)$.
	}
\end{bt}

\begin{bt}%[2D1K1]
	Tìm $m$ để hàm số $y = {x^3} + 3{x^2} + \left( {m + 1} \right)x + 4m$ đồng biến trên mỗi khoảng $\left( { - \infty ; - 2} \right)$ và $\left( {2; + \infty } \right)$.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có $y'=3x^2+6x+m+1$.
		
		Hàm số đồng biến trên $(-\infty;-2)$ và $(2;+\infty)$ khi và chỉ khi $y'\geqslant 0,\forall x\in (-\infty;-2)\cup (2;+\infty) $.
		
		Hay $3x^2+6x+m+1 \geqslant 0,\forall x\in (-\infty;-2)\cup (2;+\infty) \Leftrightarrow m \geqslant -3x^2-6x-1,\forall x\in (-\infty;-2)\cup (2;+\infty)\ (7)$.
		
		Xét hàm số $f(x)=-3x^2-6x-1$ trên $(-\infty;-2]\cup [2;+\infty)$ có $f'(x)=-6x-6; f'(x)=0\Leftrightarrow x=-1$.
		
		Bảng biến thiên \begin{center}
			\begin{tikzpicture}
			\tikzset{on double/.style = {fill = \tkzTabDefaultBackgroundColor}} 
			\tikzset{h style/.style = {pattern=north west lines}} 
			\tkzTabInit[nocadre,lgt=1.5,espcl=3]
			{$x$ /.7,$f'(x)$ /.7, $f(x)$ /2}
			{$-\infty$,$-2$,$2$,$+\infty$}
			\tkzTabLine{,+,d,h,d,+,}
			\tkzTabVar{-/$-\infty$,+DH/$-1$,D+/$-25$,-/$-\infty$}
			\end{tikzpicture}
		\end{center}
		
		Từ bảng biến thiên ta có $(7)\Leftrightarrow m\geqslant -1$.
		
		Vậy với $m\geqslant -1$, hàm số đồng biến trên $(-\infty;-2)$ và $\left( {2; + \infty } \right)$.
	}
\end{bt}

\begin{bt}%[2D1G1]
	Tìm $a$ để hàm số $y = {x^3} - 3\left( {a - 1} \right){x^2} + 3(a - 2)x + 1$ đồng biến trên mỗi khoảng có hoành độ thỏa $1 \leq  \left| x \right| \leq  2$.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có $1\leq  |x| \leq  2\Leftrightarrow x\in [-2;-1]\cup [1;2]$.
		
		Đạo hàm $y'=3x^2-6(a-1)x+3(a-2)=a(-6x+3)+3x^2+6x-6$.
		
		Hàm số đồng biến trên $[-2;-1]$ và $[1;2]$ khi và chỉ khi $y'\geqslant 0,\forall x\in [-2;-1]\cup [1;2]$.
		
		Trên $[-2;-1] $ ta có $y'\geqslant 0 \Leftrightarrow a(-6x+3)+3x^2+6x-6\geqslant 0\Leftrightarrow a\geqslant \dfrac{x^2+2x-2}{2x-1}\,\,\ (1)$.
		
		Trên $[1;2] $ ta có $y'\geqslant 0 \Leftrightarrow a(-6x+3)+3x^2+6x-6\geqslant 0\Leftrightarrow a\leq  \dfrac{x^2+2x-2}{2x-1}\,\,\ (2)$.
		
		Xét hàm số $f(x)=\dfrac{x^2+2x-2}{2x-1}$ trên $[-2;-1]\cup [1;2]$.
		
		Ta có $f'(x)=\dfrac{2x^2-2x+2}{(2x-1)^2}>0,\forall x\in [-2;-1]\cup [1;2]$.
		
		Do đó $(1)\Leftrightarrow a\geqslant f(-1)\Leftrightarrow 1$ và $(2)\Leftrightarrow a\leq  f(1)\Leftrightarrow a\leq  1$.
		
		Kết hợp ta có $a=1$, hàm số đồng biến khi $1\leq  |x|\leq  2$.
	}
\end{bt}

\begin{dang}{Tìm điều kiện của tham số để hàm số bậc ba có khoảng đơn điệu có độ dài cho trước}
	Để hàm số $y=ax^3+bx^2+cx+d$ có độ dài khoảng đồng biến ($a<0$); nghịch biến ($a>0$) $(x_1;x_2)$ bằng $l$
	\begin{itemize}
		\item \underline{Bước 1:} Tính $y^\prime$.
		\item \underline{Bước 2:} Tìm điều kiện của tham số để hàm số có khoảng đồng biến và nghịch biến 
		$$\begin{cases}
		a\neq 0\\
		\Delta >0.
		\end{cases}\,\,\,(1)$$ 
		\item \underline{Bước 3:} Biến đổi $|x_2-x_1|=l\,\,\,(2)$ thành $(x_1+x_2)^2-4x_1\cdot x_2=l^2$.
		\item \underline{Bước 4:} Sử dụng định lí Vi-ét đưa $(2)$ thành phương trình theo tham số.
		\item \underline{Bước 5:} Giải phương trình, so sánh với điều kiện $(1)$ để chọn kết quả thỏa mãn.
	\end{itemize}
\end{dang}
\begin{vd}%[2D1K1]
	Tìm $a$ để hàm số $y = {x^3} + 3{x^2} + ax + a$ nghịch biến trên đoạn có độ dài bằng 1.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có: $y' = 3{x^2} + 6x + a$; $\Delta'_{y'} = 9 - 3a $.
		
		Với $9 - 3a \leq  0 \Leftrightarrow a \geqslant 3 \Rightarrow y' \geqslant 0,\forall \in \mathbb{R}\Rightarrow  $ hàm số luôn đồng biến trên $\mathbb{R}$, mâu thuẫn giả thiết.
		
		Do đó $a \geqslant 3$ không thỏa mãn yêu cầu bài toán.
		
		Với $9 - 3a > 0 \Leftrightarrow a < 3 \Rightarrow y'$ có hai nghiệm $ {x_1},{x_2}\left( {{x_1} < {x_2}} \right) $.
		
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[ lgt=1.3, espcl=2.5]{$x$ /1,$f'(x)$ /.7,$f(x)$ /1.5}
			{$-\infty$,$x_1$,$x_2$,$+\infty$}
			\tkzTabLine{,+,$0$,-,$0$,+}
			\tkzTabVar{-/$-\infty$,+/$y_1$,-/$y_2$,+/$+\infty$}
			\end{tikzpicture}
		\end{center}
		
		
		Từ bảng biến thiên suy ra hàm số nghịch biến trên đoạn có độ dài bằng 1 khi và chỉ khi\\
		$\left| {{x_1} - {x_2}} \right| = 1 \Leftrightarrow {\left( {{x_1} + {x_2}} \right)^2} - 4{x_1}\cdot{x_2} = 1  \Leftrightarrow 4 - \dfrac{{4a}}{3} = 1 \Leftrightarrow a = \dfrac{9}{4} \text{ (thỏa mãn)}$.
		
		Vậy với $a = \dfrac{9}{4}$, hàm số đã cho nghịch biến trên đoạn có độ dài bằng 1.
	}
\end{vd}
\begin{center}
	\textbf{BÀI TẬP TỰ LUYỆN}
\end{center}
\begin{bt}%[2D1K1]
	Tìm $m$ để hàm số $ y = \dfrac{1}{3}\left( {m + 1} \right){x^3} + \left( {2m - 1} \right){x^2} - \left( {3m + 2} \right)x + m$ nghịch biến trên đoạn có độ dài bằng 4.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Với $m=-1$, ta có $y=-3x^2+x-1$ nên không thể nghịch biến trên đoạn có độ dài bằng 4.
		
		
		Với $m\ne 1$, ta có $y' = (m+1)x^2+2(2m-1)x-(3m+2)$.
		
		Suy ra $\Delta'_{y'} = (2m-1)^2+(m+1)(3m+2)=7m^2+m+3>0,\forall m\in \mathbb{R}$.
		
		Khi đó giả sử $y'$ có hai nghiệm $x_1,x_2\ \left( x_1<x_2\right)$ ta có $x_1+x_2=-\dfrac{2(2m-1)}{m+1},x_1\cdot x_2=-\dfrac{3m+2}{m+1}$.
		
		Hàm số nghịch biến trên đoạn có độ dài bằng 4 khi và chỉ khi $m>-1$ và $ \left| {{x_1} - {x_2}} \right| = 4$.
		
		Bình phương hai vế được ${\left( {{x_1} + {x_2}} \right)^2} - 4{x_1}\cdot {x_2} = 16  \Leftrightarrow \dfrac{4(2m-1)^2}{(m+1)^2}+\dfrac{4(3m+2)}{m+1}=16$.
		
		Hay $4m^2-4m+1+3m^2+5m+2=4m^2+8m+4\Leftrightarrow 3m^2-7m-1=0\Leftrightarrow m=\dfrac{7\pm \sqrt{61}}{6} \text{ (thỏa mãn)}$.
		
		Vậy với $m=\dfrac{7\pm \sqrt{61}}{6}$, hàm số đã cho nghịch biến trên đoạn có độ dài bằng 4.
		
	}
\end{bt}

\begin{bt}%[2D1K1]
	Tìm $m$ để hàm số $ y = - \dfrac{1}{3}{x^3} + {x^2} + \left( {3m + 2} \right)x + m - 3$ đồng biến trên đoạn có độ dài nhỏ hơn 4.
	\loigiai{Tập xác định của hàm số $\mathscr{D}=\mathbb{R}$.\\
		Ta có $y'=-x^2+2x+3m+2$; $\Delta'_{y'}=3m+3$.
		
		Với $m\leq  -1$, ta có $y'\leq  0,\forall x\in \mathbb{R}$ nên không thỏa mãn yêu cầu bài toán.
		
		Với $m>-1$, giả sử $y'$ có hai nghiệm $x_1,x_2\ (x_1<x_2)$, ta có $x_1+x_2=2,x_1\cdot x_2=-3m-2$.
		
		Hàm số đồng biến trên đoạn có độ dài nhỏ hơn 4 khi và chỉ khi $\left|x_1-x_2\right|\leq  4$.
		
		Bình phương hai vế được ${\left( {{x_1} + {x_2}} \right)^2} - 4{x_1}\cdot{x_2} \leq  16 \Leftrightarrow 12m+12\leq  16 \Leftrightarrow m\leq  \dfrac{1}{3}$.
		
		Kết hợp ta có $m\in \left(-1;\dfrac{1}{3}\right]$, hàm số đồng biến trên đoạn có độ dài nhỏ hơn 4.
	}
\end{bt}
\begin{center}
\textbf{BÀI TẬP TỔNG HỢP}	
\end{center}

\begin{bt}%[Nguyễn Thành Sơn]%[2D1Y1]
	Tìm khoảng đơn điệu của hàm số $y=-x^3+3x^2+4$.
	\loigiai{
		Tập xác định $\mathscr{D} = \mathbb{R}$\\
		Đạo hàm $y'=-3x^2+6x=-3x(x-2)$ và $y'=0\Leftrightarrow x=2; x=0$.
		Các giá trị $y(0)=4$, $y(2)=8$.\\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture} 
			\tikzset{double style/.append style = {draw=\tkzTabDefaultWritingColor,double=\tkzTabDefaultBackgroundColor,double distance=2pt}}
			\tkzTabInit[lgt=1.5,espcl=3]
			{$x$/1,$y'$/0.75,$y$/2}
			{$-\infty$, $0$, $2$,$+\infty$}
			\tkzTabLine{ ,-,0, +,0,-,}
			\tkzTabVar{+/$+\infty$,-/$4$,+/$8$ ,-/$-\infty$}
			\end{tikzpicture}
		\end{center}
		Vậy hàm số đồng biến trên $\left(0;2\right)$ và nghịch biến trên $\left(- \infty;0 \right)$ và $\left(2; + \infty \right)$.
	}
\end{bt} 
\begin{bt}%[Nguyễn Thành Sơn]%[2D1Y1]
	Xét tính đơn điệu của hàm số $y=x^3-3x+2$ trên tập xác định.
	\loigiai{
		Tập xác định $\mathscr{D} = \mathbb{R}$\\
		Đạo hàm $y'=3x^2-3=3(x^2-1)$ và $
		y'=0\Leftrightarrow   x=1; x=-1$. \\
		Các giá trị $y(-1)=4$, $y(1)=0$. \\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture} 
			\tikzset{double style/.append style = {draw=\tkzTabDefaultWritingColor,double=\tkzTabDefaultBackgroundColor,double distance=2pt}}
			\tkzTabInit[lgt=1.5,espcl=3]
			{$x$/1,$y'$/0.75,$y$/2}
			{$-\infty$, $-1$, $1$,$+\infty$}
			\tkzTabLine{ ,+,0, -,0,+,}
			\tkzTabVar{-/$-\infty$,+/$4$,-/$0$ ,+/$+\infty$}
			\end{tikzpicture}
		\end{center}
		Vậy hàm số đồng biến trên $\left(-\infty;-1\right)$ và $\left(1;+\infty\right)$, nghịc biến trên khoảng $\left(-1; 1 \right)$.	
	}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1B1]
	Xét tính đơn điệu của hàm số $y=\sqrt{2+x-x^2}$.
	\loigiai{
		Tập xác định $\mathscr{D}=[-1;2]$
		\\Đạo hàm $y'=\dfrac{1-2x}{2\sqrt{2+x-x^2}}$ và $
		y'=0\Leftrightarrow \dfrac{1-2x}{2\sqrt{2+x-x^2}}=0\Leftrightarrow x=\dfrac{1}{2}$.
		Giá trị $y\left(\dfrac{1}{2}\right)=\dfrac{3}{2}$\\
		Bảng biến thiên
		\begin{center}
			\begin{tikzpicture} 
			\tikzset{double style/.append style = {draw=\tkzTabDefaultWritingColor,double=\tkzTabDefaultBackgroundColor,double distance=2pt}}
			\tkzTabInit[lgt=1.5,espcl=3]
			{$x$/1,$y'$/0.75,$y$/2}
			{$-1$, $\dfrac{1}{2}$,$2$}
			\tkzTabLine{ d,+,0,-,d}
			\tkzTabVar{-/$0$,+/$\dfrac{3}{2}$,-/$0$}
			\end{tikzpicture}
		\end{center}
		Vậy hàm số đồng biến trên $\left(-1;\dfrac{1}{2}\right)$; nghịch biến trên $\left(\dfrac{1}{2};2\right)$ }		
\end{bt} 
\begin{bt}%[Nguyễn Thành Sơn]%[2D1B1]
	Xét tính đơn điệu của hàm số $y=\dfrac{2x-1}{x-1}$.
	\loigiai{
		Tập xác định $\mathscr{D} = \mathrm{R} \setminus\left\{1\right\}$
		\\Đạo hàm $y'=\dfrac{-1}{(x-1)^2}$ và $y'<0,\,\forall x\in \mathscr{D}$.
		\\Giới hạn $\lim\limits_{x\to 1^{\pm}}\dfrac{2x-1}{x-1}=\pm\infty,\lim\limits_{x\to\pm\infty}\dfrac{2x-1}{x-1}=2$
		\\Bảng biến thiên
		\begin{center}
			\begin{tikzpicture} 
			\tkzTabInit[lgt=1.5,espcl=3]
			{$x$/1,$y'$/0.75,$y$/2}
			{$-\infty$, $1$,$+\infty$}
			\tkzTabLine{ , -, d, -,}
			\tkzTabVar{+/$2$,-D+/$-\infty$/$+\infty$,-/$2$ }
			\end{tikzpicture}
		\end{center}
		Vậy hàm số luôn nghịch biến trên mỗi khoảng $\left(-\infty;1\right)$ và $\left(1;+\infty\right)$.}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Xét tính đơn điệu của hàm số $y=\sqrt{x-1}+\sqrt{3-x}$.
	\loigiai{
		Tập xác định $\mathscr{D}=\left[1;3\right]$.
		\\Đạo hàm $y'=\dfrac{1}{2\sqrt{x-1}}-\dfrac{1}{2\sqrt{3-x}}=\dfrac{\sqrt{3-x}-\sqrt{x-1}}{2\sqrt{\left(x-1\right)\left(3-x\right)}}=\dfrac{4-2x}{2\sqrt{\left(x-1\right)\left(3-x\right)}\left(\sqrt{3-x}+\sqrt{x-1}\right)}$.\\
		$y'=0\Leftrightarrow x=2\ ; y(2)=2\ ;y'$ không xác định tại $x=1$ và $x=3$.
		\\Giới hạn $\lim\limits_{x\to 1^+}y=\sqrt{2}$; $\lim\limits_{x\to 3^-}y=\sqrt{2}$.
		\\Bảng biến thiên: 
		\begin{center}
			\begin{tikzpicture} 
			\tikzset{double style/.append style = {draw=\tkzTabDefaultWritingColor,double=\tkzTabDefaultBackgroundColor,double distance=2pt}}
			\tkzTabInit[lgt=1.5,espcl=3]
			{$x$/1,$y'$/0.75,$y$/2}
			{$1$, $2$,$3$}
			\tkzTabLine{ d,+,0,-,d}
			\tkzTabVar{-/$\sqrt{2}$,+/$2$,-/$\sqrt{2}$}
			\end{tikzpicture}
		\end{center}
		
		Vậy, hàm số đồng biến trên khoảng $\left(1;2\right)$ và nghịch biến trên khoảng $\left(2;3\right)$.}
\end{bt} 
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $y=x^3-2x^2+mx+1$ đồng biến trên $\mathbb{R}$.
	\loigiai{
		Tập xác định $\mathscr{D}=\mathbb{R}$.\\
		Đạo hàm $y'=3x^2-4x+m\Rightarrow y'\ge 0\Leftrightarrow 3x^2-4x\ge -m $\\
		Hàm số đồng biến trên $\mathbb{R}$ khi $-m\le 3x^2-4x =g(x)$ với $\forall x\in \mathbb{R}$.\\
		$\Leftrightarrow - m\le \min\limits_{\mathbb{R}} g(x) =g\left(\dfrac{2}{3}\right)=-\dfrac{4}{3}\Rightarrow m\ge \dfrac{4}{3}$
	}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $y=\dfrac{1}{3}x^3+(m+1)x^2-(m+1)x+1$ đồng biến trên tập xác định của nó. 
	\loigiai{
		Tập xác định $\mathbb{D} =\mathbb{R}$\\
		Đạo hàm $y'=x^2+2(m+1)x-(m+1)$.\\
		Hàm số đồng biến trên tập xác định khi $y'\ge 0$ với $\forall x\in \mathbb{R}$.\\
		$\Leftrightarrow x^2+2(m+1)x-(m+1)\ge 0 $ với $\forall x\in \mathbb{R}$\\
		$\Leftrightarrow \Delta'= (m+1)^2 +(m+1)\le 0 \Leftrightarrow -2\le m\le -1$.}
\end{bt} 
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $y=mx^3+mx^2+(m-1)x-3$ đồng biến trên $\mathbb{R}.$
	\loigiai{
		Tập xác định $\mathscr{D}=\mathbb{R}$\\
		Đạo hàm $y'= 3mx^2+2mx+m-1$\\
		Để hàm số đồng biến trên $\mathbb{R}$ thì $y'\ge 0$ với $\forall x\in \mathbb{R}$.\\
		Ta xét các trường hợp sau:
		\begin{itemize}
			\item Trường hợp $m=0 \Rightarrow y'=-1<0$ với $\forall x\in \mathbb{R}\Rightarrow$ Hàm số nghịch biến trên $\mathbb{R}$.\\ $\Rightarrow m=0$ không thỏa yêu cầu.
			\item Trường hợp $m\ne 0$, khi đó điều kiện để hàm số đồng biến trên $\mathbb{R}$ là $\heva{&m>0\\ &\Delta'=m^2-3m(m-1)\le 0}$\\
			$\Leftrightarrow \heva{& m>0\\ &-2m^2+3m\le 0}\Leftrightarrow m\in \left[\dfrac{3}{2}; +\infty\right)$
		\end{itemize}
	}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tập hợp tất cả các giá trị thực của tham số $m$ để hàm số $y=mx-(m+1)\cos x$ đồng biến trên $\mathbb{R}.$
	\loigiai{
		Tập xác định $\mathscr{D} =\mathbb{R}$\\
		Đạo hàm $y'=m+(m+1)\sin x\Rightarrow y'\ge 0\Leftrightarrow m+(m+1)\sin x\ge 0\quad (1)$\\
		Hàm số đã cho đồng biến trên $\mathbb{R}$ nếu $y'\ge 0$ với $\forall x\in \mathbb{R}$.\\
		Ta xét các trường hợp sau:
		\begin{itemize}
			\item Trường hợp 1: $m=-1\Rightarrow (1)$ vô nghiệm $\Rightarrow m=-1$ không thỏa yêu cầu.
			\item Trường hợp 2: $m>-1$, khi đó $(1)\Leftrightarrow \sin x\ge -\dfrac{m}{m+1}$\\
			Để $y'\ge 0$ với $\forall x\in \mathbb{R}$ thì $-\dfrac{m}{m+1}\le -1\Leftrightarrow m\ge m+1$ (vô nghiệm).
			\item Trường hợp 3: $m<-1$, khi đó $(1)\Leftrightarrow \sin x\le -\dfrac{m}{m+1}$\\
			Để $y'\ge 0$ với $\forall x\in \mathbb{R}$ thì $-\dfrac{m}{m+1}\ge 1 \Leftrightarrow -m\le m+1 \Leftrightarrow m\ge -\dfrac{1}{2}$ (vô nghiệm)
		\end{itemize}
	}
\end{bt} 
\begin{bt}%[Nguyễn Thành Sơn]%[2D1G1]
	Tìm tất cả các giá trị của tham số $m$ để hàm số $y=x+m(\sin x+ \cos x)$ đồng biến trên ${\mathbb R}$. 
	\loigiai{
		Tập xác định $\mathscr{D}= \mathbb{R}$
		\\Đạo hàm $y'=1+m(\cos x- \sin x)$ 
		\\Hàm số đồng biến trên $\mathbb{R}$ thì $y' \ge 0$ với mọi $x \in \mathbb{R}$.\\
		Khi đó $\sqrt{2}m\cos\left(x+\dfrac{\pi}{4}\right) \ge -1$ với mọi $x \in \mathbb{R}.$\\
		$\bullet$ Nếu $m = 0$, ta có kết luận đúng.\\
		$\bullet$ Nếu $m>0$, ta có $\sqrt{2}\cos\left(x+\dfrac{\pi}{4}\right) \ge -\dfrac{1}{m}$ với mọi $x \in \mathbb{R} \Leftrightarrow -\sqrt{2} \ge -\dfrac{1}{m} \Leftrightarrow  0<m \le \dfrac{\sqrt{2}}{2}.$\\
		$\bullet$ Nếu $m<0$, ta có $\sqrt{2}\cos\left(x+\dfrac{\pi}{4}\right) \le -\dfrac{1}{m}$ với mọi $x \in \mathbb{R} \Leftrightarrow \sqrt{2} \le -\dfrac{1}{m} \Leftrightarrow  0> m \ge -\dfrac{\sqrt{2}}{2}.$
		\\Vậy $-\dfrac{\sqrt{2}}{2} \le m \le \dfrac{\sqrt{2}}{2}.$
	}
\end{bt} 
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $y=\dfrac{mx-4}{x-m}$ nghịch biến trên $(0;+\infty)$.
	\loigiai{$\circ$ Tập xác định: $\mathscr{D}= \mathbb{R}\setminus\left\{m\right\}$.\\
		Đạo hàm: $y'=\dfrac{-m^2+4}{\left(x-m\right)^2}$. \\
		Hàm số nghịch biến trên $(0;+\infty)$ khi và chỉ khi
		\begin{eqnarray*}
			\heva{&m\le 0\\&y'<0\ \forall x\in \left(0;+\infty\right)}\Leftrightarrow \heva{&m\le 0\\&4-m^2<0}\Leftrightarrow \heva{&m\le 0\\&m^2>4}\Leftrightarrow m<-2.
		\end{eqnarray*}
		Với $m \in \left( { - \infty ; - 2} \right)$ thì hàm số nghịch biến trên $(0;+\infty)$.}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $y=\dfrac{mx+1}{x+m}$ đồng biến trên $(2;+\infty)$.
	\loigiai{Tập xác định $\mathscr{D} =\mathbb{R} \setminus \{m\}$.\\
		$y'=\dfrac{m^2-1}{(x+m)^2}$, $x\neq -m$.\\
		Hàm số $y=\dfrac{mx+1}{x+m}$ đồng biến trên $(2;+\infty)$ khi và chỉ khi $y'>0$ với mọi $x \in \left(2;+\infty\right)$\\
		$\Leftrightarrow \heva{&-m \leq 2\\ &m^2-1 >0}\Leftrightarrow \hoac{&\heva{&m \geq -2\\ &m<-1 }\\& m>1} \Leftrightarrow m \in \left[-2;1\right) \cup \left(1;+\infty\right).$
	}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1G1]
	Tìm tập hợp các giá trị thực của tham số $m$ để hàm số $y=-mx^3+x^2-3x+m-2$ nghịch biến trên $(-3;0)$.
	\loigiai{
		Tập xác định $\mathscr{D} =\mathbb{R}$.\\
		Đạo hàm $y'=-3mx^2+2x-3$.\\
		Hàm số nghịch biến trên khoảng $(-3; 0)$ khi $y'\le 0$ với $\forall x\in (-3; 0)$\\
		$y'\le 0 \Leftrightarrow -3mx^2+2x-3\le 0 \quad (1)$\\
		Do $x\in (-3; 0)$ nên $(1)\Leftrightarrow -3m\le -\dfrac{2}{x}+\dfrac{3}{x^2} =\left(\dfrac{3}{x}+1\right)\left(\dfrac{1}{x}-3\right)+1$\\
		Mặt khác $g(x)=\left(\dfrac{3}{x}+1\right)\left(\dfrac{1}{x}-3\right)> 0$ với $\forall x\in (-3; 0)$ và $\lim\limits_{x\to -3} g(x)=0$.\\
		Nên $y'\le 0$ với $\forall x\in (-3; 0)\Leftrightarrow -3m\le 1\Leftrightarrow m\ge -\dfrac{1}{3}$
		
	}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1G1]
	Tìm tập hợp tất cả các giá trị của tham số $m$ sao cho hàm số $y=\dfrac{x+1}{x^2+x+m}$ nghịch biến trên khoảng $(-1;1)$.
	\loigiai{
		Điều kiện $x^2 + x + m \ne 0$.\\
		Đạo hàm $y'=\dfrac{-x^2-2x-1+m}{(x^2+x+m)^2}$\\
		Hàm số nghịch biến trên khoảng $(-1; 1)$ nếu:
		$\heva{&-x^2-2x-1+m\le 0, \forall x\in (-1; 1) &\quad (1)\\ &x^2+x+m\ne 0, \forall x\in (-1; 1)&\quad(2)}$\\
		$ (1)\Leftrightarrow m\le (x+1)^2$ với $\forall x\in (-1; 1) \Leftrightarrow m\le 0$.\\
		Xét hàm số $g(x)=x^2+x$ trên khoảng $(-1; 1)$, có $g'(x)=2x+1=0\Leftrightarrow x=-\dfrac{1}{2}$.\\
		Bảng biến thiên: 
		\begin{center}
			\begin{tikzpicture}
			\tkzTabInit[lgt=1,espcl=3]
			{$x$ /.9, $y'$ /.7,$y$ /2}
			{$-1$,$-\dfrac{1}{2}$,$1$}
			\tkzTabLine{,-,$0$,+}
			\tkzTabVar{ +/$0$,-/$-\dfrac{1}{4}$,+/$2$}
			\end{tikzpicture}
		\end{center}
		$\Rightarrow$ Phương trình $g(x)=-m$ vô nghiệm trên khoảng $(-1; 1)$ khi $\hoac{&-m\ge 2\\ &-m\le -\dfrac{1}{4}}\Leftrightarrow \hoac{&m\le -2\\ &m\ge \dfrac{1}{4}}$\\
		$\Rightarrow (2)\Leftrightarrow \hoac{&m\le -2\\ &m\ge \dfrac{1}{4}}$\\
		$\Rightarrow$ Với $m\le -2$ thì số đã cho nghịch biến trên khoảng $(-1; 1)$
	}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tập hợp tất cả các giá trị của $m$ để hàm số $y=x^3+3x^2-mx-4$ đồng biến trên khoảng $(-\infty; 1)$.
	\loigiai{ 
		Tập xác định: $\mathscr{D} = \mathbb{R}$.\\
		Ta có $y'=3x^2+6x-m$, yêu cầu bài toán tương đương $m\le 3x^2+6x$, $\forall x\in (-\infty;1]\Leftrightarrow m\le \min\limits_{(-\infty;1]} \left(3x^2+6x\right)=-3$}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị của tham số $m$ để hàm số $y=x^3 -2x^2-(m-1)x+2$ đồng biến trên $\left(0;+\infty \right).$ 
	\loigiai{Ta có $y'=3x^2-4x-(m-1)$. \\ 
		Yêu cầu bài toán tương đương $3x^2-4x-m+1\ge 0\forall x>0\Leftrightarrow m\le 3x^2-4x+1\forall x>0\Leftrightarrow m\le \min\limits_{(0;+\infty)} (3x^2-4x+1)$. \\ 
		Lập bảng biến thiên ta có $\min\limits_{(0;+\infty)} (3x^2-4x+1)=-\dfrac{1}{3}$.}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $f(x)=\dfrac{2x^2+3x+m+1}{x+1}$ đồng biến trên mỗi khoảng xác định.
	\loigiai{
		Ta có: TXĐ $\mathscr{D} = \mathbb{R} \setminus \{ -1 \}$.\\
		$f'(x)=\dfrac{2x^2+4x+2-m}{(x+1)^2}$. \\
		Để $f(x)$ đồng biến trên TXĐ $ \Rightarrow f'(x)>0$, $\forall x \ne -1$. \\
		$\begin{aligned} 	& \Leftrightarrow 2x^2+4x+2-m>0 \\ 	& \Leftrightarrow \left\{\begin{aligned} 	&a>0 \\  & \Delta < 0 	\end{aligned}\right. \Leftrightarrow 2m< 0 \Leftrightarrow m < 0. \end{aligned}$. }
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $y=\dfrac{mx+4}{x+m}$ đồng biến trên $\left(1;+\infty \right)$. \\
	\loigiai{
		TXĐ: $\mathscr{D}=\mathbb{R}\setminus \left\{-m\right\}$, $y'=\dfrac{m^2-4}{(x+m)^2}$. \\
		Hàm số đồng biến trên $\left(1;+\infty \right)$ khi $\left\{\begin{aligned}
		&y'>0,\forall x\in \left(1;+\infty \right) \\
		&-m\notin \left(1;+\infty \right)
		\end{aligned}\right. \Leftrightarrow \left\{\begin{aligned}
		&m^2-4>0 \\ &-m \leq 1 \end{aligned}\right. \Leftrightarrow \left\{\begin{aligned}
		&m>2\vee m<-2 \\ &m\geq -1 \end{aligned}\right. \Leftrightarrow m>2$. }
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $y=\sqrt{x^2-x+1}-mx$ đồng biến trên $\mathbb{R}$.
	\loigiai{ \\
		Tập xác định: $\mathscr{D} = \mathbb{R}$. \\
		$y'=\dfrac{2x-1}{2\sqrt{x^2-x+1}}-m$. \\
		Hàm số đồng biến trên $\mathbb{R}$ $ \Leftrightarrow y'\geq 0;\forall x\in \mathbb{R}$ $ \Leftrightarrow m \leq \dfrac{2x-1}{\sqrt{(2x-1)^2+3}};\forall x\in \mathbb{R} \hfill (1)$. \\
		Xét hàm số $f(t)=\dfrac{t}{\sqrt{t^2+3}}$ có $f'(t)=\dfrac{3}{\sqrt{\left(t^2+3 \right)^3}}>0;\forall t\in \mathbb{R}$ và $\displaystyle\lim \limits_{t\to -\infty}f(t)=-1$. \\
		Do đó: $(1) \Leftrightarrow m \leq -1$. }
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1G1]
	Tìm tập hợp tất cả các giá trị thực của tham số $m$ để hàm số $y=mx+(m+1)\sqrt{x-2}$ nghịch biến trên $\mathscr{D}=\left[2;+\infty \right)$.
	\loigiai{
		Ta có: $y=mx+(m+1)\sqrt{x-2} \Rightarrow y'=m+\dfrac{m+1}{2\sqrt{x-2}}$, $y'$ xác định trên khoảng $\left(2;+\infty \right)$. \\
		Nhận xét: khi $x$ nhận giá trị trên $\left(2;+\infty \right)$ thì $\dfrac{1}{2\sqrt{x-2}}$ nhận mọi giá trị trên $\left(0;+\infty \right)$. \\
		Yêu cầu bài toán $ \Leftrightarrow y'\leq 0,\forall x\in \left(2;+\infty \right) \Leftrightarrow (m+1)t+m\leq 0,\forall t\in \left(0;+\infty \right)$ $\left( \text{đặt } t=\dfrac{1}{2\sqrt{x-2}} \right)$. \\
		$ \Leftrightarrow \left\{\begin{aligned}
		&m+1\leq 0 \\
		&m+(m+1)\cdot 0\leq 0
		\end{aligned}\right. \Leftrightarrow m\leq -1$. }
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1G1]
	`Tìm tất cả giá trị thực của tham số $m$ sao cho hàm số $y=f(x)=\dfrac{m-2\sin x}{1+\cos^2x}$ nghịch biến trên khoảng $\left(0;\dfrac{\pi}{6}\right)$.
	\loigiai{
		Ta có: $y'=\dfrac{-2\cos x\left(\sin^2x+2-m\sin x\right)}{{\left(1+\cos^2x\right)}^2}$. \\
		Vậy $y'\leq 0\,\forall x\in \left(0;\dfrac{\pi}{6}\right) \Leftrightarrow \sin^2x+2-m\sin x\geq 0\,\forall x\in \left(0;\dfrac{\pi}{6}\right)$. \\
		$ \Leftrightarrow m\leq \dfrac{\sin^2x+2}{\sin x} \forall x\in \left(0;\dfrac{\pi}{6}\right)$. \\
		Đặt $t=\sin x \Rightarrow t\in \left(0;\dfrac{1}{2}\right)$. \\
		Vậy $ \Leftrightarrow m\leq \dfrac{t^2+2}{t}=g(t) \forall t\in \left(0;\dfrac{1}{2}\right)$. \\
		Ta có: $\displaystyle\min \limits_{\left(0;\dfrac{1}{2}\right)}g(t)=\dfrac{9}{2}$. Vậy $m\leq \dfrac{9}{2}$. }
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1G1]
	Tìm tất cả các giá trị của tham số $m$ để hàm số $y=\dfrac{\cos x+1}{2\cos x-m}$ đồng biến trên $\left(0;\dfrac{\pi}{2}\right)$.
	\loigiai{
		$y=\dfrac{\cos x+1}{2\cos x-m} \Rightarrow y'=\dfrac{(m+2)sinx}{{\left(2\cos x-m\right)}^2}$. \\
		Vì $\sin \,x\ne 0\,\forall x\in \left(0;\dfrac{\pi}{2}\right)$ nên hàm đồng biến trên $\left(0;\dfrac{\pi}{2}\right)$ khi và chỉ khi \\
		$\left\{\begin{aligned} &m+2>0 \\ &\left[\begin{aligned} &\dfrac{m}{2}\leq 0 \\
		&\dfrac{m}{2}\geq 1 \end{aligned}\right. \end{aligned}\right. \Leftrightarrow \left\{\begin{aligned} &m>-2 \\  &\left[\begin{aligned} &m<0 \\ &m>2 \end{aligned}\right.
		\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
		&-2<m\leq 0 \\
		&m>2
		\end{aligned}\right. $}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1G1]
	Cho hàm số $y=\dfrac{(m-1)\sin x-2}{\sin x-m}$. Tìm tất cả các giá trị của tham số $m$ để hàm số nghịch biến trên khoảng $\left(0;\dfrac{\pi}{2}\right)$.
	\loigiai{
		Điều kiện: $\sin x\ne m$. \\
		Điều kiện cần để hàm số $y=\dfrac{(m-1)\sin x-2}{\sin x-m}$ nghịch biến trên khoảng $\left(0;\dfrac{\pi}{2}\right)$ là $\left[\begin{aligned}
		&m\geq 1 \\ &m\leq 0 \end{aligned}\right. $. \\
		Ta có: $y'=\dfrac{(2+m-m^2)\cos x}{{(\sin x-m)}^2}$.\\ 
		Ta thấy $\dfrac{\cos x}{{(\sin x-m)}^2}>0$ $\forall x\in \left(0;\dfrac{\pi}{2} \right)$. \\
		Để ham số $y=\dfrac{(m-1)\sin x-2}{\sin x-m}$ nghịch biến trên khoảng $\left(0;\dfrac{\pi}{2}\right)$ là $\left\{\begin{aligned}
		&y'<0 \\ &\left[\begin{aligned} &m\geq 1 \\  &m\leq 0 \end{aligned}\right.
		\end{aligned}\right. \Leftrightarrow \left\{\begin{aligned} &2+m-m^2<0 \\
		&\left[\begin{aligned} &m\geq 1 \\ &m\leq 0 \end{aligned}\right. \end{aligned}\right. $. \\ $ \Leftrightarrow \left\{\begin{aligned}
		&\left[\begin{aligned} &m>2 \\ &m<-1 \end{aligned}\right. \\
		&\left[\begin{aligned} &m\geq 1 \\ &m\leq 0 \end{aligned}\right.
		\end{aligned}\right. \Leftrightarrow \left[\begin{aligned} &m>2 \\
		&m<-1 \end{aligned}\right. $. }
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $y=\dfrac{\cot x-1}{m\cot x-1}$ đồng biến trên khoảng $\left(\dfrac{\pi}{4};\dfrac{\pi}{2}\right)$.
	\loigiai{
		Ta có: $y'=\dfrac{-\left(1+\cot^2x\right)\left(m\cot x-1\right)+m\left(1+\cot^2x\right)\left(\cot x-1\right)}{{\left(m\cot x-1\right)}^2}\,=\dfrac{\left(1+\cot^2x\right)(1-m)}{{\left(m\cot x-1\right)}^2}$. \\
		Hàm số đồng biến trên khoảng $\left(\dfrac{\pi}{4};\dfrac{\pi}{2}\right)$ khi và chỉ khi \\
		$\left\{\begin{aligned}
		&m\cot x-1\ne 0, \forall x\in \left(\dfrac{\pi}{4};\dfrac{\pi}{2}\right) \\
		&y'=\dfrac{\left(1+\cot^2x\right)(1-m)}{{\left(m\cot x-1\right)}^2}>0,\forall x\in \left(\dfrac{\pi}{4};\dfrac{\pi}{2}\right)
		\end{aligned}\right. \Leftrightarrow \left\{\begin{aligned}
		&m\leq 0 \vee m\geq 1 \\
		&1-m>0
		\end{aligned}\right. \Leftrightarrow m\leq 0$}
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tập hợp các giá trị của tham số $m$ để hàm số $y=\sqrt{x^2+1}-mx-1$ đồng biến trên khoảng $\left(-\infty;+\infty \right)$.
	\loigiai{ \\
		Tập xác định: $\mathscr{D} = \mathbb{R}$.
		$y'=\dfrac{x}{\sqrt{x^2+1}}-m$. \\
		Hàm số đồng biến trên $(-\infty;+\infty )$ khi và chỉ khi: $y'\geq 0,\forall x\in \mathbb{R}$ $ \Leftrightarrow \dfrac{x}{\sqrt{x^2+1}}\geq m,\forall x\in \mathbb{R}$. (1). \\
		Xét hàm số $f(x)=\dfrac{x}{\sqrt{x^2+1}}$, ta có $f'(x)=\dfrac{\sqrt{x^2+1}-\dfrac{x^2}{\sqrt{x^2+1}}}{{\left(\sqrt{x^2+1}\right)}^2}=\dfrac{1}{{\left(\sqrt{x^2+1}\right)}^3}>0,\forall \in \mathbb{R}$. \\
		Suy ra $f(x)$ đồng biến trên $\mathbb{R}$. \\
		Mặt khác, $\displaystyle\lim \limits_{x\to -\infty}f(x)=-1,\displaystyle\lim \limits_{x\to -\infty}f(x)=1$. Từ đó, $(1) \Leftrightarrow m\leq -1$. }
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm các giá trị của tham số $m$ để hàm số $y=-\dfrac{1}{3}x^3+(m-1)x^2+(m+3)x-10$ đồng biến trên khoảng $(0;3).$
	\loigiai{
		Ta có: $y'=-x^2+2(m-1)x+m+3=g(x)$ \\
		Do $y$ là hàm số bậc ba với hệ số $a<0$ nên hàm số đồng biến trên $\left(0; 3\right) \Leftrightarrow y'=0$ có hai nghiệm $x_1$, $x_2$ thỏa $x_1\leq0<3\leq  x_2$ $ \Leftrightarrow \left\{\begin{aligned}
		&-1\cdot g(0)\leq 0 \\  &-1\cdot g(3)\leq 0 \end{aligned}\right. $ $ \Leftrightarrow \left\{\begin{aligned}  &m+3\geq 0 \\ &7m-12\geq 0 \end{aligned}\right. \Leftrightarrow m\geq \dfrac{12}{7}$ }
\end{bt}
\begin{bt}%[Nguyễn Thành Sơn]%[2D1K1]
	Tìm tất cả các giá trị thực của tham số $m$ để hàm số $y=\dfrac{x^2-4x}{x+m}$ đồng biến trên $\left[1;+\infty \right)$.
	\loigiai{
		Tập xác định: $\mathscr{D}=\mathbb{R}\setminus \left\{-m\right\}$ và $y'=\dfrac{x^2+2mx-4m}{(x+m)^2}$. \\
		Hàm số đã cho đồng biến trên $\left[1;+\infty \right) \Leftrightarrow \left\{\begin{aligned}
		&-m<1 \\
		&x^2+2mx-4m\geq 0,\forall x\in \left[1;+\infty \right)
		\end{aligned}\right. $ \\
		$x^2+2mx-4m\geq 0,\forall x\in \left[1;+\infty \right) \Leftrightarrow \left[\begin{aligned}
		& \Delta \leq 0 \\ 
		&\left\{\begin{aligned}
		& \Delta >0 \\
		&x_1<x_2\leq 1
		\end{aligned}\right.
		\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
		&m^2+4m\leq 0 \\
		&\left\{\begin{aligned}
		&m^2+4m>0 \\
		&-m+\sqrt{m^2+4m}\leq 1
		\end{aligned}\right.
		\end{aligned}\right. \Leftrightarrow \left[\begin{aligned}
		&-4\leq m \leq 0 \\
		&\left\{\begin{aligned}
		&\left[\begin{aligned}
		&m>0 \\
		&m<-4
		\end{aligned}\right. \\
		&m\geq -1 \\
		&m\leq \dfrac{1}{2}
		\end{aligned}\right.
		\end{aligned}\right. $ \\
		Kết hợp với điều kiện $m>-1$ ta được $-1<m\leq \dfrac{1}{2}$. }
\end{bt}
