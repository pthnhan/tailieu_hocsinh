\begin{ex}%[2D1G5-5]
	Cho đồ thị của ba hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$ được vẽ mô tả ở hình dưới đây. Hỏi đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong nào?
	\choice
	{\True $(C_1);(C_2);(C_3)$}
	{$(C_2);(C_1);(C_3)$}
	{$(C_3);(C_2);(C_1)$}
	{\True $(C_3);(C_1);(C_2)$}
	\loigiai{
		\begin{center}
			\begin{tikzpicture}[line join=round, line cap=round,>=stealth,scale=0.7]
			\def\xmin{-4}\def\xmax{4}\def\ymin{-4}\def\ymax{4}
			\draw[thick,->] (\xmin-0.6,0)--(\xmax+0.6,0) node[above] {\footnotesize $x$};
			\draw[thick,->] (0,\ymin-0.6)--(0,\ymax+0.6) node[right] {\footnotesize $y$};
			\draw (-0.1,0) node [below right] {\footnotesize $O$};
			\foreach \x in {\xmin,...,\xmax}{\ifthenelse{\not \x=0}{
					\draw (\x,-0.1)--(\x,0.1) node [above] {\footnotesize $\x$};}}
			\foreach \y in {\ymin,...,-1} {\draw (-0.1,\y)--(0.1,\y) node [right] {\footnotesize $\y$};}
			\foreach \y in {1,...,\ymax}{\draw (0.1,\y)--(-0.1,\y) node [left] {\footnotesize $\y$};}
			\clip (\xmin-0.5,\ymin-0.5) rectangle (\xmax+1.5,\ymax+0.5);
			\draw[smooth,samples=200,thick,domain=\xmin-0.5:\xmax+0.3] plot(\x,{cos(2*\x r) }) node[right] {$ C_3 $};
			\draw[smooth,samples=200,thick,domain=\xmin-0.5:\xmax+0.3,color=red] plot(\x,{-2*sin(2*\x r) }) node[right] {$ C_1 $};
			\draw[smooth,samples=200,thick,domain=\xmin-0.5:\xmax+0.3,color=blue] plot(\x,{4*cos(2*\x r) }) node[right] {$ C_2 $};
			%	\draw[dashed] (-1,0) -- (-1,-2) -- (0,-2);
			%	\draw[dashed] (0,1) -- (0.5,1);
			\end{tikzpicture}
		\end{center}
		Gọi $(C)$ là đồ thị hàm số $y=g(x)$, $(C')$ là đồ thị hàm số $y=g'(x)$, ta có
		\begin{itemize}
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía dưới trục hoành thì $(C)$ là đường đi xuống.
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía trên trục hoành thì $(C)$ là đường đi lên.
		\end{itemize}
		Quan sát đồ thị ta thấy ứng với các khoảng mà đồ thị $(C_1)$ nằm trên trục hoành thì đồ thị $(C_3)$ “đi lên” và ngược lại; còn ứng với các khoảng mà đồ thị $(C_2)$ nằm trên trục hoành thì đồ thị $(C_1)$ “đi lên” và ngược lại.\\
		Do đó, đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong $(C_3)$; $(C_1)$; $(C_2)$. }
\end{ex}

\begin{ex}%[2D1G5-5]
	Cho đồ thị của ba hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$ được vẽ mô tả ở hình dưới đây. Hỏi đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong nào?
	\choice
	{\True $(C_1);(C_2);(C_3)$}
	{$(C_1);(C_3);(C_2)$}
	{$(C_3);(C_2);(C_1)$}
	{$(C_2);(C_3);(C_1)$}
	\loigiai{
			\begin{center}
			\begin{tikzpicture}[line join=round, line cap=round,>=stealth,scale=0.7]
			\def\xmin{-4}\def\xmax{4}\def\ymin{-4}\def\ymax{4}
			\draw[thick,->] (\xmin-0.6,0)--(\xmax+0.6,0) node[below] {\footnotesize $x$};
			\draw[thick,->] (0,\ymin-0.6)--(0,\ymax+0.6) node[right] {\footnotesize $y$};
			\draw (-0.1,0) node [below right] {\footnotesize $O$};
			\foreach \x in {\xmin,...,\xmax}{\ifthenelse{\not \x=0}{
					\draw (\x,-0.1)--(\x,0.1) node [above] {\footnotesize $\x$};}{}}
			\foreach \y in {\ymin,...,-1} {\draw (-0.1,\y)--(0.1,\y) node [right] {\footnotesize $\y$};}
			\foreach \y in {1,...,\ymax}{\draw (0.1,\y)--(-0.1,\y) node [left] {\footnotesize $\y$};}
			\clip (\xmin-0.5,\ymin-0.5) rectangle (\xmax+1.5,\ymax+0.5);
			\draw[smooth,samples=200,thick,domain=\xmin-0.5:\xmax+0.3] plot(\x,{sin(2*\x r) }) node[right] {$ C_2 $};
			\draw[smooth,samples=200,thick,domain=\xmin-0.5:\xmax+0.3,color=red] plot(\x,{2*cos(2*\x r) }) node[right] {$ C_3 $};
			\draw[smooth,samples=200,thick,domain=\xmin-0.5:\xmax+0.3,color=blue] plot(\x,{-4*sin(2*\x r) }) node[right] {$ C_1 $};
			\end{tikzpicture}
		\end{center}
		Gọi $(C)$ là đồ thị hàm số $y=g(x)$, $(C')$ là đồ thị hàm số $y=g'(x)$, ta có
		\begin{itemize}
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía dưới trục hoành thì $(C)$ là đường đi xuống.
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía trên trục hoành thì $(C)$ là đường đi lên.
		\end{itemize}
		Quan sát đồ thị ta thấy ứng với các khoảng mà đồ thị $(C_3)$ nằm trên trục hoành thì đồ thị $(C_2)$ “đi lên” và ngược lại; còn ứng với các khoảng mà đồ thị $(C_1)$ nằm trên trục hoành thì đồ thị $(C_3)$ “đi lên” và ngược lại.\\
		Do đó, đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong $(C_1)$; $(C_2)$; $(C_3)$.}
\end{ex}

\begin{ex}%[2D1G5-5]
	Cho đồ thị của ba hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$ được vẽ mô tả ở hình dưới đây. Hỏi đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong nào?
	\begin{center}
		\begin{tikzpicture}[line join=round, line cap=round,>=stealth,scale=1]
		\def\xmin{-3}\def\xmax{3}\def\ymin{-1}\def\ymax{2}
		\draw[thick,->] (\xmin-0.6,0)--(\xmax+1,0) node[below] {\footnotesize $x$};
		\draw[thick,->] (0,\ymin-0.6)--(0,\ymax+0.6) node[right] {\footnotesize $y$};
		\draw (0,0) node [above right] {\footnotesize $O$};
		\foreach \x in {\xmin,...,\xmax}{\ifthenelse{\not \x=0}{
				\draw (\x,0.1)--(\x,-0.1)  ;}{}}
		\foreach \y in {\ymin,...,\ymax} {
			\ifthenelse{\not \y=0}{\draw (0.1,\y)--(-0.1,\y)  ;}{} }
		%	\clip (\xmin-0.5,\ymin-0.5) rectangle (\xmax+1,\ymax+0.9);
		\draw[smooth,samples=200,thick,domain=-4.5:4,color=blue] plot (\x,{1/((\x+1/2)^2+1)+1/8}) node[above] {$ b $};
		\draw[smooth,samples=200,thick,domain=-4.5:4,color=red] plot (\x,{(-(2.0*(\x)+1.0))/(((\x)+1.0/2.0)^(2.0)+1.0)^(2.0)});
		\draw (1,-0.3)node[below] {$c$};
		\draw[smooth,samples=200,thick,domain=-4.5:4] plot (\x,{rad(atan(\x+1/2))+\x/8+1})node[right] {$ a $};
		
		
		\end{tikzpicture}
	\end{center}
	\choice
	{\True $a$, $b$, $c$}
	{$b$, $a$, $c$}
	{$a$, $c$, $b$}
	{$b$, $c$, $a$}
	\loigiai{
			Gọi $(C)$ là đồ thị hàm số $y=g(x)$, $(C')$ là đồ thị hàm số $y=g'(x)$, ta có
		\begin{itemize}
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía dưới trục hoành (đạo hàm  $g'(x)<0$) thì $(C)$ là đường đi xuống.
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía trên trục hoành (đạo hàm  $g'(x)>0$) thì $(C)$ là đường đi lên.
		\end{itemize}
		Trên khoảng mà đường $(c)$ nằm trên trục hoành thì đường $(b)$ đi lên từ trái sang phải, trên khoảng mà đường $(c)$ nằm dưới trục hoành thì đường $(b)$ đi xuống từ trái sang phải. \\
		Đường $(b)$ luôn nằm trên trục hoành và đường $a$ luôn đi lên từ trái sang phải.\\
		Từ đó suy ra đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong $a$, $b$, $c$.
		}
\end{ex}

\begin{ex}%[2D1G5-5]
	Cho đồ thị của ba hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$ được vẽ mô tả ở hình dưới đây. Hỏi đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong nào?
	\begin{center}
		\begin{tikzpicture}[line join=round, line cap=round,>=stealth,scale=0.7]
		\def\xmin{-1}\def\xmax{4}\def\ymin{-4}\def\ymax{4}
		\draw[thick,->] (\xmin-0.6,0)--(\xmax+0.6,0) node[below] {\footnotesize $x$};
		\draw[thick,->] (0,\ymin-0.6)--(0,\ymax+0.6) node[right] {\footnotesize $y$};
		\draw (-0.1,0) node [below left] {\footnotesize $O$};
		\clip (\xmin-0.5,\ymin-0.5) rectangle (\xmax+1.5,\ymax+0.5);
		\draw[smooth,samples=200,thick,domain=0:3.5] plot(\x,{-cos((3/2)*\x r)*sin(\x r) }) node[right] {$a$};
		\draw[smooth,samples=200,thick,domain=0:3.5,color=red] plot(\x,{(3/2)*sin((3/2)*\x r)*sin(\x r)-cos((3/2)*\x r)*cos(\x r)}) node[right] {$c$};
		\draw[smooth,samples=200,thick,domain=0:3.5,color=blue] plot(\x,{(13/4)*cos((3/2)*\x r)*sin(\x r) +3*sin((3/2)*\x r)*cos(\x r)}) node[right] {$b$};
		
		\end{tikzpicture}
	\end{center}
	\choice
	{$a$, $b$, $c$}
	{$b$, $a$, $c$}
	{\True $a$, $c$, $b$}
	{$b$, $c$, $a$}
	\loigiai{
			Gọi $(C)$ là đồ thị hàm số $y=g(x)$, $(C')$ là đồ thị hàm số $y=g'(x)$, ta có
		\begin{itemize}
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía dưới trục hoành (đạo hàm  $g'(x)<0$) thì $(C)$ là đường đi xuống.
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía trên trục hoành (đạo hàm  $g'(x)>0$) thì $(C)$ là đường đi lên.
		\end{itemize}
		Trên khoảng mà đường $(b)$ nằm trên trục hoành thì đường $(c)$ đi lên từ trái sang phải, trên khoảng mà đường $(b$ nằm dưới trục hoành thì đường $(c)$ đi xuống từ trái sang phải. \\
		Trên khoảng mà đường $(c)$ nằm trên trục hoành thì đường $(a)$ đi lên từ trái sang phải, trên khoảng mà đường $(c)$ nằm dưới trục hoành thì đường $(a)$ đi xuống từ trái sang phải. \\
		Từ đó suy ra đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong $a$, $c$, $b$.
	}
\end{ex}

\begin{ex}%[2D1G5-5]
	Cho đồ thị của ba hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$ được vẽ mô tả ở hình dưới đây. Hỏi đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong nào?
		\begin{center}
		\begin{tikzpicture}[line join=round, line cap=round,>=stealth,scale=0.7]
		\def\xmin{-4}\def\xmax{4}\def\ymin{-4}\def\ymax{4}
		\draw[thick,->] (\xmin-0.6,0)--(\xmax+0.6,0) node[below] {\footnotesize $x$};
		\draw[thick,->] (0,\ymin-0.6)--(0,\ymax+0.6) node[right] {\footnotesize $y$};
		\draw (-0.1,0) node [above right] {\footnotesize $O$};
		\clip (\xmin-0.5,\ymin-0.5) rectangle (\xmax+1.5,\ymax+0.5);
		\draw[smooth,samples=200,thick,domain=-4:4] plot(\x,{(12*(\x)^2-4)/(((\x)^2+1)^3)});
		\draw (1,1.2) node[right] {$b$};
		\draw[smooth,samples=200,thick,domain=-4:4,color=red] plot(\x,{(-4*(\x))/(((\x)^2+1)^2)});
		\draw (1,-1.2) node[right] {$c$};
		\draw[smooth,samples=200,thick,domain=-4:4,color=blue] plot(\x,{2/((\x)^2+1)+1});
		\draw (1,2) node[right] {$a$};
		
		\end{tikzpicture}
	\end{center}
	\choice
	{$a$, $b$, $c$}
	{$b$, $a$, $c$}
	{\True $a$, $c$, $b$}
	{$b$, $c$, $a$}
	\loigiai{	Gọi $(C)$ là đồ thị hàm số $y=g(x)$, $(C')$ là đồ thị hàm số $y=g'(x)$, ta có
		\begin{itemize}
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía dưới trục hoành (đạo hàm  $g'(x)<0$) thì $(C)$ là đường đi xuống.
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía trên trục hoành (đạo hàm  $g'(x)>0$) thì $(C)$ là đường đi lên.
		\end{itemize}
	Do đó, đồ thị các hàm số $y=f(x)$, $y=f'(x)$ và $y=f''(x)$ theo thứ tự, lần lượt tương ứng với đường cong $a$, $c$, $b$.}
\end{ex}

\begin{ex}%[2D1G5-5]
	Cho đồ thị của bốn hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$, $y=f'''(x)$ được vẽ mô tả ở hình dưới đây. Hỏi đồ thị các hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$ và $y=f'''(x)$ theo thứ tự, lần lượt tương ứng với đường cong nào?
	\begin{center}
		\begin{tikzpicture}[line join=round, line cap=round,>=stealth,scale=0.7]
		\def\xmin{-3.5}\def\xmax{3.5}\def\ymin{-3}\def\ymax{3.5}
		\draw[thick,->] (\xmin-0.6,0)--(\xmax+0.6,0) node[below] {\footnotesize $x$};
		\draw[thick,->] (0,\ymin-0.6)--(0,\ymax+0.6) node[right] {\footnotesize $y$};
		\draw (-0.1,0) node [below left] {\footnotesize $O$};
		\clip (\xmin-0.5,\ymin-0.5) rectangle (\xmax+1.5,\ymax+0.5);
		\draw[smooth,samples=200,thick,domain=-2:2] plot(\x,{3*(\x)^2}); \draw(0.5,1) node[left] {$a$};
		\draw[smooth,samples=200,thick,domain=-3:3,color=red] 	plot(\x,{(\x)^3});
		\draw(1.2,1) node[left] {$b$};
		\draw[smooth,samples=200,thick,domain=-3:3,color=blue] 	plot(\x,{0.25*((\x)^4)-1});
		\draw(1.5,1) node[right] {$c$};
		\draw[smooth,samples=200,thick,domain=-3:3,color=brown] 	plot(\x,{0.05*((\x)^5)-\x});
		\draw(2.2,1) node[right] {$d$};
		
		\end{tikzpicture}
	\end{center}
	\choice
	{$c$, $d$, $b$, $a$}
	{\True $d$, $c$, $b$, $a$}
	{$d$, $c$, $a$, $b$}
	{$d$, $b$, $c$, $a$}
	\loigiai{
	Gọi $(C)$ là đồ thị hàm số $y=g(x)$, $(C')$ là đồ thị hàm số $y=g'(x)$, ta có
\begin{itemize}
	\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía dưới trục hoành (đạo hàm  $g'(x)<0$) thì $(C)$ là đường đi xuống.
	\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía trên trục hoành (đạo hàm  $g'(x)>0$) thì $(C)$ là đường đi lên.
\end{itemize}
 Từ đó, ta suy ra đồ thị các hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$ và $y=f'''(x)$ theo thứ tự, lần lượt tương ứng với đường cong $d$, $c$, $b$, $a$.}
\end{ex}

\begin{ex}%[2D1G5-5]
	Cho đồ thị của bốn hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$, $y=f'''(x)$ được vẽ mô tả ở hình dưới đây. Hỏi đồ thị các hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$, $y=f'''(x)$ theo thứ tự, lần lượt tương ứng với đường cong nào?
	\choice
	{$c$, $d$, $b$, $a$}
	{$d$, $c$, $a$, $b$}
	{\True $d$, $c$, $b$, $a$}
	{$d$, $b$, $c$, $a$}
		\begin{center}
			\begin{tikzpicture}[line join=round, line cap=round,>=stealth,scale=1]
			\def\xmin{-1}\def\xmax{5}\def\ymin{-2}\def\ymax{4}
			\draw[thick,->] (\xmin-0.6,0)--(\xmax+0.8,0) node[below] {\footnotesize $x$};
			\draw[thick,->] (0,\ymin-0.6)--(0,\ymax+0.6) node[right] {\footnotesize $y$};
			\draw (0,0) node [below left] {\footnotesize $O$};
			\clip (\xmin-0.5,\ymin-0.5) rectangle (\xmax+1,\ymax+0.9);
			\draw[smooth,samples=200,thick,domain=0:5,color=blue] plot (\x,{1/((\x-1)^2+1)+1/4}) node[right] {$c$};
			\draw[smooth,samples=200,thick,domain=0:5,color=red] plot (\x,{(-2*(\x-1)/(((\x)-1)^2+1.0)^2});
			\draw (2,-0.5)node[below] {$b$};
			\draw[smooth,samples=200,thick,domain=0:5] plot (\x,{rad(atan(\x-1))+\x/4+13/16})node[above right] {$d$};
			\draw[smooth,samples=200,thick,domain=0:5,color=brown] plot (\x,{(-2/(((\x)-1)^2+1)^2+(8*(\x-1)^2/(((\x)-1)^2+1)^(3))});
			\draw (1,-2) node[right] {$a$};
			
			\end{tikzpicture}
		\end{center}
	\loigiai{
		Gọi $(C)$ là đồ thị hàm số $y=g(x)$, $(C')$ là đồ thị hàm số $y=g'(x)$, ta có
		\begin{itemize}
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía dưới trục hoành (đạo hàm  $g'(x)<0$) thì $(C)$ là đường đi xuống.
			\item Trên khoảng $(a;b)$ mà đồ thị $(C')$ nằm phía trên trục hoành (đạo hàm  $g'(x)>0$) thì $(C)$ là đường đi lên.
		\end{itemize}
		Từ đó, ta suy ra đồ thị các hàm số $y=f(x)$, $y=f'(x)$, $y=f''(x)$, $y=f'''(x)$ theo thứ tự, lần lượt tương ứng với đường cong $d$, $c$, $b$, $a$.}
\end{ex}
